# UrbanPhotoMeshQA 迭代优化 1

> 唯一目标：提高单个城市摄影测量 glTF Mesh 的无参考质量评估准确性，不再为检索、身份不变性、零水印或其他下游任务折中。
>
> 固定协议：单种子 2026；只使用 Val 选模；唯一候选冻结前不加载、不评测 Test/Blind；新结果写入新的 ablations/ 或 final/ 子目录，不覆盖 v1。

## 状态

- [ ] 未开始
- [-] 正在执行
- [x] 已验收
- [!] 存在阻断问题

## P0：数据真实性

### 1. 修复几何攻击导出的大坐标 float32 量化伪影

**状态：[x]**

源 glTF 通过 node matrix 保存香港大坐标。当前流程把节点变换展开到约 8.4e5 的世界坐标，再写入 float32 POSITION。远端实测 float32 ULP 为 0.0625 m；孔洞样本中本应不变的顶点产生约 2.4–3.4 cm 中位偏移、约 4.3 cm 最大偏移。

修复内容：

1. 攻击计算保留 float64 世界坐标。
2. 导出时将 POSITION 写为稳定原点附近的局部坐标，并把原点写入 node translation/matrix。
3. 保留源材质、纹理 sampler、alpha 和 base-color 语义，避免几何攻击附带改变外观。
4. 增加大坐标往返、未攻击顶点精度和材质保留回归测试。

Pilot 验收：

- 选择3个不同尺寸和面数的 Train/Val 模型；
- 生成3模型×3几何攻击×3等级，共27个新包；
- 孔洞保留顶点的额外误差不高于 1e-4 m；
- 轻度尖刺没有被量化吞掉；
- 所有包可独立重载，依赖与材质完整；
- 相邻等级不再因坐标精度出现并列。

验收结果（2026-09-03）：

- 低/中/高复杂度3个模型的27个包全部通过；
- 最大坐标往返误差为 2.6574e-6 m；
- 最大局部POSITION绝对值为51.866 m，不再直接存储8.4e5量级世界坐标；
- 9组三级攻击的文件摘要均唯一，几何强度均单调；
- 材质语义和源贴图字节全部保持。

### 2. 建立几何 v2 数据

**状态：[x]**

- Pilot 通过后才执行。
- 冻结 v1，只重建受影响的几何攻击包和缓存，复用现有纹理攻击包。
- 使用新目录、新 manifest、新 asset digest 和新审计报告。
- 严格采用3493条正式规则，不带回3条QEM重复项。

验收结果（2026-09-03）：

- v2目录新生成1656个几何包，v1目录未修改；
- 全量坐标/依赖/材质审计完成，3条已知QEM heavy按原正式规则排除；
- 正式1653条几何记录最大坐标往返误差为7.9556e-6 m；
- 新提取1653套原始缓存，全部成功；
- 与复用的1840套Clean/纹理缓存按v2 Manifest合并为3493/3493条；
- 已生成机器路径版和data-root-relative便携版Manifest及缓存审计。

### 2B. 修复局部纹理攻击脱离实际UV使用区

**状态：[x]**

首次224分辨率正式纹理真值审计发现：旧版区域缺失会随机落入整张atlas，旧版错位只寻找整图高方差区域；二者均未约束攻击区域必须被三角面UV使用。因此分别有41个和35个建筑至少存在一个六视角完全不可见的等级，其中包含攻击落入atlas padding的非预期no-op。

修复原则：

- 从 glTF 的 Face—Material—Texture—TEXCOORD_0 关系生成每张贴图的UV使用掩码；
- 区域缺失围绕UV已使用且非128灰色的texel定位，错位围绕UV已使用的水平纹理边缘定位；
- 同一建筑/攻击的三级强度保持相同中心，只改变范围、位移或混合强度；
- 无有效纹理内容、即使直接比较UV表面采样也完全不变的真实no-op继续显式标记，不伪造退化；
- 在新目录只重建区域缺失与错位，保留旧数据和纹理细节损失包。

验收结果（2026-09-03）：

- 新生成1104个UV-aware区域缺失/错位包；与1656个几何v2包共同存放于独立v2目录，原始模型和v1攻击目录均未覆盖；
- v3正式Manifest保持3493条及1518/760/607/608划分不变；
- 最终区域缺失客观no-op为0；错位no-op由旧版104条降至45条；
- 剩余错位no-op与15个纹理细节损失no-op建筑的UV使用区确为常量纹理，保留为质量1的等价Clean样本。

## P1：质量真值

### 3. 重建几何客观真值

**状态：[x]**

- 配对评测统一使用 Clean 的中心和尺度，不再独立归一化后比较。
- 将1024点的点到点距离升级为确定性高密度点到三角面距离。
- 分别保存 Chamfer、稳健 Hausdorff、法向、完整度、拓扑等原始量。
- 验收重测一致性、三级单调性和轻度缺陷可分性。

验收结果（2026-09-03）：

- 从原始Clean/Attacked glTF直接计算，不使用1024点缓存；
- 在Clean公共中心/尺度下，每侧确定性采样8192点，并使用点到三角面精确最近点；
- 正式生成1837条目标（184条Clean＋1653条几何退化），16项原始度量全部有限；
- 四个split的有序键与v2 Manifest逐条一致，30条重复计算最大绝对差为0；
- 主退化量三级严格单调率：孔洞183/184、QEM 176/184、尖刺183/184；
- 保留10组真实的非单调原始测量，不篡改观测值；标量质量阶段使用累计严重度包络；
- 4096/8192/16384采样Pilot后固定8192，核心指标相对16384的秩相关约0.998—1.000。

### 4. 重建纹理客观真值

**状态：[x]**

- Pilot 比较160和224渲染尺寸，替代当前96像素的过低分辨率。
- 指标只在有效前景和纹理可见区域计算，不让背景稀释退化。
- 补充结构/感知指标，同时记录依赖和许可证。
- 将纹理攻击中客观质量等于1的不可见/no-op样本显式标记，不把它们强行作为可分类攻击。

阶段性结论（2026-09-03）：

- 160/224三建筑Pilot均通过，固定224；核心指标跨分辨率SRCC约0.96—1.00，224额外检出一个轻度区域缺失；
- 使用材质语义正确的六视角渲染，并显式输出前景和纹理可见掩码；
- 增加按三角面表面积确定性采样、经Face—UV回到原贴图的全表面纹理度量，避免遮挡面被六视角漏检；
- 首轮旧纹理资产的1840条诊断真值已完整生成并通过重复性审计，但因暴露P0-2B问题，仅作为诊断记录，不作为最终v2真值。

验收结果（2026-09-03）：

- 最终生成1840条纹理真值（184 Clean＋1656纹理攻击），11项原始指标全部有限；
- 224像素六视角有效纹理区域最少15173像素；每个建筑另采样65536个全表面UV点；
- 32768/65536采样核心指标SRCC为0.9998—1.000，中位相对误差低于0.9%；
- 30条从glTF重算的最大绝对差为0，no-op标志完全一致；
- 主指标三级单调率：细节损失182/184、区域缺失184/184、错位184/184；原始非单调值保留供审计。

### 5. 定义 OQI v2

**状态：[x]**

- 不再默认使用“Train P95截断后等权平均”作为最终质量定义。
- 明确报告 Geometry Fidelity、Texture Fidelity、Topology Health、Completeness。
- 第一阶段采用事先固定、可解释的权重；如以后引入MOS，只能用Train/Val拟合。
- 保留OQI v1作对照，新定义使用新的schema version。

验收结果（2026-09-03）：

- 输出Geometry Fidelity、Texture Fidelity、Topology Health、Completeness以及组合后的Geometry/Texture/Overall Quality；
- 各分项采用显式固定尺度与权重的指数失真映射，公式仅参考Geometry Train与三建筑纹理Pilot后冻结；
- Overall取Geometry与Texture的短板，不使用攻击类型或强度作为输入；
- 90条真实纹理no-op保持质量1并排除矛盾排序，所有可观测三级序列经累计负担包络后100%单调；
- 3493条全部有限、位于[0,1]、顺序与正式Manifest一致，四个split数量不变。

## P2：质量专用 Base

### 6. 比较 identity 与归一化前 global 特征

**状态：[x]**

- Point/Mesh比较现有identity输出和global输出，不再默认只使用按检索目标训练的单位向量。
- 首轮冻结编码器，只加载Train/Val。
- 报告分攻击、分等级的特征敏感性及Val质量指标。

验收结果（2026-09-03）：

- 使用几何专项子集隔离纹理分支干扰：Train 798、Val 400；未加载Test/Blind；
- 同一次GPU提取保存Point/Mesh的identity与global，维度分别为Point 256/521、Mesh 256/1032，避免重复读取和重复编码；
- 同头部、同损失、同种子对照中，global的Val OQI为MAE 0.208、PLCC 0.593、SRCC 0.605，identity为MAE 0.224、PLCC 0.545、SRCC 0.556；
- 仅用于诊断的Clean/Attack特征距离中，global与客观失真程度的Val SRCC为0.568，identity为0.415；三级严重度距离单调率分别为119/120和99/120；
- 因此下一阶段采用global作为几何Base输入。该结论目前只覆盖几何子集，纹理空间表征仍在P3第9项单独处理；
- 训练器已兼容v1/v2客观真值文件、过滤样本键对齐和缺少Patch真值的情况，空攻击集合不再产生NaN。

### 7. 质量感知部分解冻

**状态：[x]**

- 依次解冻融合层、最后局部编码块；不直接全量解冻。
- Base使用较小学习率，并启用梯度裁剪、早停和分支学习率。
- Val选模同时约束OQI、Geometry和Texture，不允许靠牺牲一个分项晋级。

几何阶段结果（2026-09-03）：

- 以冻结global候选为起点，Head/Base学习率分别为2e-4/1e-5，梯度裁剪1.0，早停耐心15；
- 只解冻Mesh融合层：Val OQI MAE 0.205、SRCC 0.599，因排序性能低于冻结global的0.605而淘汰；
- 解冻Point最后局部块、Mesh最后Face块及融合层：Val OQI MAE 0.204、PLCC 0.614、SRCC 0.622，Geometry SRCC 0.618，作为当前几何候选；
- 完全相同的小Batch与warm-start流程下，Base全冻结控制组SRCC仅0.590，排除了训练日程本身带来提升的解释；
- 两轮均只加载Train 798与Val 400，未读取Test/Blind；
- 完整多模态复验使用第9项空间纹理输入和第8项均衡共享头；全冻结同日程控制的Val OQI/Geometry/Texture SRCC为0.615/0.667/0.614；
- 最后局部块部分解冻达到Val OQI MAE 0.177、PLCC 0.676、SRCC 0.677，Geometry/Texture SRCC为0.718/0.652，三项同时提升；
- 完整实验最佳第15轮、27轮早停，仍仅加载Train 1518与Val 760。因此接受Point最后局部块、Mesh最后Face块与融合层的部分解冻，关闭本项。

## P3：模型与训练目标

### 8. 几何、纹理和Overall分路建模

**状态：[x]**

- Geometry trunk使用Point、Mesh、Morphology和几何Patch。
- Texture trunk使用保留空间信息的多视图纹理特征。
- Overall由两个分项质量和受控跨模态信息融合，不再完全共享一个回归MLP。
- 攻击类型/强度只作为低权重辅助任务，并监控梯度冲突。

验收结果（2026-09-03）：

- 在完整v3 Train 1518/Val 760上，以相同Point/Mesh global、Patch和空间纹理输入比较共享头、硬min分路、学习式Overall分路融合；未加载Test/Blind；
- 均衡共享头：Val OQI/Geometry/Texture SRCC为0.608/0.657/0.610；
- 硬min分路为0.577/0.568/0.553，学习式分路融合为0.594/0.559/0.547，均未通过；
- 模态损失去重后共享头OQI升至0.619、Texture升至0.633，但Geometry跌至0.391，触发分项退化门槛而淘汰；
- 结论：强制分路会放大分项噪声或削弱跨模态判别，当前不采用；保留均衡共享融合结构，但换用第9项空间纹理输入。该项以“完成验证并否决不优方案”关闭，而非宣称分路有效。

### 9. 保留纹理空间信息

**状态：[x]**

- 不再对六视图立即全局池化、单位归一化并简单平均。
- 保留feature map/token，加入前景池化与可学习视图注意力。
- 加入局部清晰度、频谱/梯度能量、空白区比例、视图一致性等无参考统计。
- 增加纹理像素、表面积、有效UV覆盖组成的texel-density特征。

验收结果（2026-09-03）：

- 从单个待测glTF直接生成6视图，每视图保留1个前景全局token与2×2空间token，共30×576维，并显式保存有效token mask；
- 每视图增加12项无参考外观统计，每个资产增加8项贴图像素、UV使用覆盖、表面积和texel-density统计；
- v3纹理专项Train 800、Val 400全部提取成功，四个shard签名一致、错误0，未加载Test/Blind；
- 同一MobileNet特征源对照：pooled/spatial/spatial+stats的Val Texture SRCC分别为0.636/0.672/0.725，三级严重度单调率为55.8%/63.3%/80.0%；
- spatial+stats分攻击SRCC：细节损失0.697、区域缺失0.782、错位/重影0.573；选择spatial+stats进入第8项完整分路模型，错位类继续作为重点短板。

### 10. 按建筑组织训练和排序

**状态：[x]**

- batch按建筑成组，承认1518条记录实际只有80个独立Train建筑。
- 排序损失约束同一建筑、同一攻击内部的 Clean > Light > Medium > Heavy。
- 不再强制不同建筑之间的任意代理OQI成对排序。
- 缩小Head，增强正则与早停。

验收结果（2026-09-03）：

- 每个batch包含4栋建筑的完整Clean与攻击集合，排序损失只比较同一建筑内部，未加载Test/Blind；
- 相比随机batch，Val Geometry SRCC由0.657升至0.698，Texture由0.610升至0.628；
- 但主指标OQI SRCC由0.6082微降至0.6075，MAE由0.226变差至0.232，因此按既定Val门槛不晋级；
- 结论：组内排序对分项有益，但当前Overall目标未同步受益。本轮保留为消融，不继续搜索batch建筑数，正式候选仍使用随机batch均衡共享头。

### 11. 跨图幅选择

**状态：[x]**

- 在5个Train图幅上执行确定性的leave-one-tile-out内部验证。
- 正式Val只承担候选模型最终选择，减少对单一Val图幅的反复过拟合。
- 同时记录平均Train-tile、最差Train-tile和正式Val指标。

验收结果（2026-09-03）：

- 从正式Train内部构建5个确定性leave-one-tile-out fold，每折训练1214–1215条、验证303–304条；正式Val/Test/Blind均未加载；
- 为避免内部验证泄漏，每折空间纹理头从头训练，未使用看过完整Train的纹理Checkpoint；
- OQI SRCC五折均值0.591、最差0.486；Geometry均值/最差0.656/0.592；Texture均值/最差0.604/0.496；
- 最差OQI图幅为11-SW-9D，说明跨图幅域偏移显著。后续候选先比较LOTO均值与最差图幅，再交由正式Val最终选择；本轮不再只围绕11-SW-4B反复迭代。

## P4：局部质量

### 12. 将几何Patch改为拓扑/测地Patch

**状态：[x]**

- 不再仅按面心欧氏距离选择最近32个面。
- 使用共享边拓扑、空间最小生成树虚拟桥和测地扩张进行面积配额分区。香港建筑模型包含大量真实断开构件，28/184个资产超过16个连通分量、最大1151个；因此严格禁止跨分量与固定16 Patch在数学上不兼容。虚拟桥只用于分区且显式记录，不修改Mesh或Face—UV关系。
- 提高密集Mesh覆盖率，并导出稳定face membership、面积和中心。
- 184/184个原始模型已生成v2侧车：逐面唯一覆盖率1.0、平均UV有效率0.999950；面积CV中位数由普通测地Voronoi的0.678降至面积配额区域生长的0.401，P90由0.987降至0.658。
- 侧车保存CSR face membership、face→patch、Patch面积/中心、真实连通分量数、虚拟桥数和源glTF哈希；全量审计位于`artifacts/quality/ablations/topological_patch_v2_seed2026/`。

### 13. 建立Patch—Face—UV—Texture监督

**状态：[x]**

- 由三角面UV和material生成每个Patch的纹理可见区域。
- 离线使用Clean/Attacked贴图差异生成Patch Texture Quality；推理仍只输入单模型。
- 正确处理UV seam、重复展开、多材质、无效UV与遮挡。
- 每个Patch输出Geometry、Texture和Overall Quality。
- 已完成正式局部真值：Patch Geometry 1837条、Patch Texture 1840条，合并后严格对齐3493条正式清单；Train/Val/Test/Blind仍为1518/760/607/608，有序样本键SHA256为`065fe44a9eff3d7410fde9e9dfce1a86041abbf99600b3c4969516bc2c5f1166`。
- 纹理标签使用六视图z-buffer可见face-id和每Patch 4096个确定性UV表面样本，沿用OQI v2六项纹理指标；正式审计的纹理监督Patch覆盖率为1.0，可比较三级序列5656/5656单调。
- 几何标签按每个Attacked Mesh自身拓扑Patch生成，Clean缺失样本通过最近Attacked三角面归属，避免把Clean Patch编号错误套到拓扑已变化的模型；不对跨强度的局部Patch编号强制单调包络。
- 正式NPZ位于外置数据目录`Targets/{patch_geometry_targets_v1_seed2026,patch_texture_targets_v1_seed2026,local_patch_quality_v1_seed2026}`。
- 已完成单模型局部特征：58维拓扑/形态描述、6个可见性约束多视角纹理token、Face—UV直接栅格化的纹理atlas token；Train/Val共2278条，36445个有效Patch全部至少有一种纹理观测。
- 仅用Train/Val训练并选择三输出局部头。最终Val Geometry/Texture/Overall SRCC为`0.381/0.602/0.422`；独立纹理分支优于view-only，几何上下文分支只用于Geometry，组合规则在解锁Test/Blind前冻结。
- `scripts/infer_real_gltf_quality.py --local-v2-checkpoint ...`已支持单个glTF输出16个Patch的Geometry、Texture、Overall、中心、面积和纹理可见性；推理不读取Clean、攻击类别或强度。
- 最终局部v2发布位于`artifacts/quality/final/local_patch_v2_seed2026/`。

## P5：工程与发布一致性

### 14. 正式数据溯源和结果统一

**状态：[x]**

- 新checkpoint必须保存3493条正式有序样本指纹。
- 所有新指标统一使用平均秩Spearman，并标记旧结果口径。
- 候选阶段只加载Train/Val；唯一候选冻结后才校准并评测Test/Blind。
- 保持单模型重提取与缓存推理数值等价。

验收结果（2026-09-03）：

- 最终Checkpoint内嵌3493条正式样本计数及有序键指纹`065fe44a9eff3d7410fde9e9dfce1a86041abbf99600b3c4969516bc2c5f1166`；Checkpoint SHA256为`3a94425fc791987db1d1c7cf3843a4be15e6acb48a3a7629c16ddf9bf38bb8ef`。
- 新训练与评测脚本的Spearman统一采用SciPy `rankdata(method="average")`处理并列秩；候选训练、消融和组合只加载Train/Val。
- 唯一候选及其哈希冻结后，才执行一次Test/Blind评测；之后未依据锁定集改变模型。Test Geometry/Texture/Overall SRCC为`0.433/0.623/0.446`，Blind为`0.468/0.578/0.432`。
- 选取一栋Train建筑的19条完整记录执行缓存与单glTF在线重提取审计，19/19通过，最终三输出最大绝对差为`0.0`。
- 外置数据、局部真值及全量特征只保存在`/Volumes/SANDISK-ELE/UrbanPhotoMeshQA-Data`；工作前必须先验证`/Volumes/SANDISK-ELE`已挂载，未挂载则停止并提醒插入移动硬盘。

## 执行顺序

P0-1导出精度修复与测试
→ P0-1三模型27包Pilot
→ P0-2几何v2
→ P1真值与OQI v2
→ P2质量专用Base
→ P3分路模型、组内排序与跨图幅选择
→ P4拓扑Patch与局部纹理监督
→ P5冻结、校准、一次性Test/Blind评测与发布

## v1对照基线

- 发布目录：artifacts/quality/final/release_seed2026_v1
- Val OQI SRCC约0.520
- Test OQI SRCC约0.557
- Blind OQI SRCC约0.444
- Blind最弱图幅11-SW-4D，OQI SRCC约0.252
- 禁止根据Test/Blind结果选择或回调任何新方法。
