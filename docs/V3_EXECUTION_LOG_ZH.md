# V3执行记录

## 2026-09-06：启动与安全修复

- 用户授权执行迭代3；先建设并验收数据，再GPU训练；仅Val迭代。
- 外置盘已挂载，约828GiB可用；GPU4090空闲，数据盘约91GiB可用，尚未清理任何其他项目。
- SSH域名在本机解析到代理假IP导致断连；Google DNS HTTPS核实地址116.136.52.182后连接成功。使用HostName覆盖及原域名HostKeyAlias，不关闭主机身份校验、不改系统代理。地址以后可能变化，失败时重新核实。
- 旧V3的assets、_review、audits、human_ratings、manifests、patch_targets、previews已原盘重命名归档到`_archive/pre_6class33_20260905T201731Z`，内有逐目录恢复映射。没有删除或重新复制旧数据。旧评分保留供追溯，不自动继承。
- 已停止原评分网站及子服务；不打开/渲染示例网页。
- 远端`repo`是历史脏工作区，不覆盖；`iteration2-main`为干净旧发布工作树。V3应使用隔离目录。
- 修复校准脚本只加载Train/Val、不提前推理Test/Blind；修复局部纹理空监督损失与空统计NaN，空指标输出null并从选模汇总排除。
- 新建6类×4＋8组合＋Clean的33槽位协议模块，固定16Patch、内容绑定、人工优先、无有效独立评分拒绝准入。
- 定向单元测试19项通过，仅CPU小测试，未运行训练或全量特征提取。
- 已建立源文件轻量清单（10图幅2485栋，尚未评级），外置盘`manifests/v3_6class33_seed2026/source_inventory.json`，未解析全量网格，未把这些候选自动标成scale5。
- 新增多空间区域共享预算与嵌套范围实现（尚需接入新生成器）；分数面积用于纹理支持，删面必须报告整面实际预算误差。
- 已启用本任务15分钟持续推进心跳`urbanphotomeshqa-3`，读取本日志接续工作；不是后台训练已启动，也不是完成承诺。旧网页服务已关闭。
- 启动代码提交`2a8f49a`已推送GitHub main；GPU隔离仓库`/root/autodl-tmp/UrbanPhotoMeshQA/iteration3-code`已从GitHub克隆，跳过LFS大文件自动下载。远端Python为`/root/autodl-tmp/mvgsage-env-current/bin/python`，新协议3项测试通过；本地定向测试最终20项通过。
- 下一次直接从6类生成器适配开始，不再重复归档、克隆或全量历史审计。`source_inventory.json`只列候选不是正式Manifest；评分证据可复用归档中的`_review/clean_reselection_20260906`，读取前校验对应源内容。远端环境是否有pymeshlab/DracoPy尚待检查。

## 未完成与下一步


### 最新接续：2026-09-06 10:52–12:05，批量筛选完成、110候选开发复核与有界补档

- 原有3栋开发资产共110版本（A44＋B33＋C33），两规格证据全部复用。正式合格仍0；3栋开发校准未通过，12栋试生产未开始，正式GPU训练未开始。
- 新原始Train池首批240包技术筛选：126符合本批技术门槛，114未选（主要有效面数不足，不是生成失败）；126栋均完成7个原生1024视角，42张派生联系表全部检查，14栋进一步原生细节查看。4栋暂定AI scale5储备：B364321787901063A0、B359481578101063A0、B357491563701063A1、B356251527601063A0。加B361511560501063A0及旧备用B421142104901063A0，共6栋Train储备，均非MOS/正式合格；其余122栋未准入。证据和理由在ratings/clean_material_v2/fresh_*decisions.json，不能重复筛这126栋。
- 99个原始dev33版本完成一次当前UV全副本像素支持回投审计，audits/uv_support_v1。256/1024面积均匀数值积分，有收敛差异记录；不宣称精确像素交、不代表可见性或局部质量，不从QEM推断精确对应。A11个修订尚未做此审计。
- GPU分别试用官方Qwen2.5-VL-7B与Qwen3-VL-8B的相同12匿名校准样本：前者全部4且全属性0，后者9个5且严重缺失漏检，均失败，停止批量评分路线。模型缓存和失败输出保留；不是QA训练。ratings/vlm_advisory_v1、qwen3_advisory_v1含原输出和失败说明，不得把这些机器输出自动准入。
- 已完成110个现有候选的目标隐藏开发复核（首12意见复用＋98新意见），先记录再关联private_mapping，报告audits/development_mixed110_diagnostic_v1.json。61/110目标匹配仅为描述性数字。评分者参与开发，不是严格独立盲评或MOS。G3均未知；纯色灰面判断存在不一致，不能据此训练T3。A Clean历史5与本轮4存在分歧，保留而非强改，详见ratings/development_mixed_v1/review_limitations.json。
- 服务器启动26个单类补档候选（B15、C11），assets/<ID>/dev_gap02_<variant>，两路受限生成＋512/1024证据，单项超时480秒/渲染180秒，失败单独保留，计划与执行器在jobs/dev_bc_gap_revision02_plan.json、run_dev_bc_gap_revision02.py。会话95416，events逐项记录；不可重复启动已完成项，不覆盖原110。G3平滑加深可能出现G2副作用，仍需真实审查。
- Mac启动第二批240个尚未筛选Train原包技术预筛，会话57825，audits/fresh_train_discovery02、_scratch/fresh_discovery02.log。冻结排除历史及首批240，未重复候选。后续仅为技术通过项生成证据。
- 新Blind原包仅下载并做CRC/SHA校验，尚未看图或调参：11-SW-14A有204栋、11-SW-15B有438栋，raw_new_blind/archives及receipt保留；另11-SW-6A无Building、6B有3栋。不是30栋Blind已选定。地理预留reservation_v2在图像筛选前记录，后续仍需邻近/重复组隔离；评测锁定。
- 代码本地/GPU为670d010，GPU历史fixture脏修改与用户启动脚本保留。旧心跳仍PAUSED，无重复自动化。下一步检查两项正在运行作业、完成补档复核与几何真实覆盖，达到3栋校准与12栋试生产门槛后才能扩展，不能因3小时目标跳过验收。


### 最新接续：2026-09-06 10:22–10:52，接管与跨建筑批量筛选/生成

- 本任务 `01a07481-8678-7061-875c-3c4cd0951389` 已先暂停旧心跳 `urbanphotomeshqa-3`，确认旧任务空闲、远端没有遗留生成/训练后接管。用户追加授权充分使用空闲Mac；所有预览、临时文件、数据和实验仍只存外置盘/GPU受控目录。未创建重复自动化，没有重新归档当前数据。
- 外置盘真实挂载；SSH使用已核验的116.136.52.182及原HostKeyAlias。GPU隔离repo保留两份历史fixture脏修改；本地“启动质量评分.command”保留。新训练未开始，正式准入仍0。
- 本轮额外完成105栋未复查Train候选的3视角512预筛，目录 `previews/discovery_train_batch03_gpu`、`batch04_mac`～`batch11_mac`；各批有冻结screening_plan、receipt、screening.json与逐栋理由。不存在用渲染成功代替scale5。补齐短名单七视角1024，详细拒绝原因在 `ratings/clean_material_v2/batch03_detail_decisions.json`、`batch04_detail_decisions.json`、`batch05_11_detail_decisions.json`。不得重复筛这些已检查资产。
- 新开发B `B414372125401063A0`（折线长板楼）、C `B412712118401063A0`（低层弧形屋顶体育馆）经七视角＋原生细节人工式AI查看，独立暂评5；记录 `ratings/clean_admission_visual_v2/<ID>.json` 绑定内容/图片hash，非MOS、非正式准入。`B361511560501063A0` 暂评5仅为Train储备，角色 `reviewed_reserve_train_only`；不是第4栋开发。均有纯色墙体/反光/遮蔽局限说明。
- B/C各33个开发候选已经在GPU生成、完成512与1024全部七视角并回传；目录 `assets/<B或C>/dev33_attempt01`、`previews/devB_33_512`/`devB_33_1024`及devC对应目录。接收包hash与当前16Patch标签绑定核验各33通过，见 `audits/<ID>_dev33_attempt01_received.json`；不要重复不变审计或渲染。A原44保留，因此当前3栋开发建筑、110个候选版本，开发校准尚未验收，正式条数0。
- Mac新技术筛选 `audits/fresh_train_discovery01`：冻结240个未在历史340列表的候选，仅来自既有Train图幅，排除历史所有Split建筑ID。126达到本批技术/至少256面门槛，114未选中（不统称生成失败）。`previews/fresh_train_detail01` 正在两并发、有180秒单栋上限地生成七视角1024；已有50栋时记录本条。事件日志可恢复，部分失败不覆盖，未评分不冒充scale5。后续还要做跨ID实体分组，不代表正式Split已冻结。
- 为避免串行视觉复核瓶颈，在独立 `iteration3-review-env` 安装Qwen2.5-VL所需环境；官方ModelScope Qwen2.5-VL-7B-Instruct权重下载到GPU数据 `model_cache`。准备只接收匿名图片的独立机器辅助复核，先在开发候选校准；不传目标/配方/旧分，不使用待训练QA模型自己预测作真值。此时权重下载中，尚无该模型评分；不能报告成训练或正式标注。
- 主要待完成：开发110版本的目标隐藏混合复核、G1/G3/T3及组合有效性、UV复用回投和有效覆盖检查、3栋校准验收、12栋Train试生产。全新Blind图幅仍未取得，原始2485栋来自10个历史使用图幅，不能换名作全新Blind。用户希望3小时推进完成，但该愿望不是质量门槛豁免或已测量完成承诺。

### 最新接续：轻档候选完成，用户再次授权持续推进

- 最新收尾：GPU代码**4f2ba19**；T1第二轮单条生成＋512/1024证据链会话75389成功退出，82297/80001/48084全部同步成功，接收校验1/1通过（`audits/A_T1_attempt02_scale2_received.json`）。已查看总览和原生view0，暂评2，另存`ratings/A_T1_attempt02_scale2/development_observation.json`。T1现在也有4/3/2/1开发候选，尚需混合复核，非正式验收。
- G2逐条补看1024斜俯视view5，新开发意见5/4/2/1，写`ratings/A_dev33_detail_v2/G2_supplemental_opinions.json`。形变主要位于屋顶/基座/长墙边缘，可能与顶点仅集中于这些部位有关，不能仅由选区数宣称立面内部覆盖好。需要实际位移、面/16Patch、空间覆盖诊断；不可为了填scale1盲目放大已严重的屋顶尖刺。
- **接续优先：** T1/T2暂停追加候选，推进G1/G3/T3及组合的已有细节复核，并检查G2实际面/顶点覆盖后只补轻中档；同时补开发Clean多样性与UV复用回投。当前没有活跃GPU生成/训练进程，不要重跑以上已完成会话。正式4950数据尚未建成，训练未启动，持续心跳保持启用。
- 继续查看T1原四条的1024原生view0，补充意见4/3/1/1：轻档由旧全景5修为4，第三档仍过重；另存`ratings/A_dev33_detail_v2/T1_supplemental_opinions.json`，明确仅view0新细节、目标知情、非正式标签。只将T1_level3降采样比.025→.045、JPEG20→25，拟生成第二轮单条`dev_T1_attempt02_scale2`；不重生成已存在的另外三档。T1第四档灰色来自极强纹理压缩观感，真实性仍须一起审查，不能凭匹配1就正式通过。
- 59047、34630、9284及两份证据同步全部成功退出，无需再等待。轻档包一次接收校验1/1通过：`audits/A_T2_attempt04_light_received.json`。
- 已查看第四轮轻档512七视角总览及1024原生view0，暂评4；外置盘`ratings/A_T2_attempt04_light/development_observation.json`保存内容/receipt绑定及理由，明确目标知情开发意见、非MOS、非正式标签。T2已有第三轮3/2/1与第四轮4的候选组合，但仍需混合复核和物理可信性验收，不称正式四档完成。
- 下一步不再盲目生成T2：推进A原33条中其余类别的已有1024补充复核，先定位尚缺档位与副作用，再有限调整；继续不同形态Clean筛选与UV回投任务。原33条数据/证据均已完整，不重复渲染。当前没有生成/训练作业运行，由已有心跳继续；正式准入0，新训练未开始。

### 最新接续：2026-09-06 09:42起，T2第三轮生成与接收校验完成

- 最新运行状态：GPU代码已到**10643df**，会话**59047**正在生成`dev_T2_attempt04_light`的T2_level1。下次检查成功退出/1条manifest后回传、一次hash核验并补512+1024证据。不要再等待已完成的7033、58203、1390、6461；新训练尚未启动。
- 本轮继续完成：旧Mac rsync不接受两个远端源写法，改成独立同步1390/6461均成功，未发生数据删除。已经逐条查看第三轮4条512七视角总览和1024原生view0，先保存意见后读映射：level1→4为**3/3/2/1，3/4目标匹配**。评分在`ratings/A_T2_attempt03_blind/independent_scores.json`，带局限的绑定报告`diagnostic_report_with_scope.json`。不是正式准入，原生细节仅view0单独查看。
- 发现历次固定shuffle令相同槽位序列可被猜测，且评分者同时参与生成，因此本批只作目标隐藏开发意见，明确记录不满足严格盲评。新队列采用seed2026＋内容digest排序，同版本跨分辨率一致、新内容改变次序；旧队列恢复保留旧顺序，新增ordering.json绑定manifest，拒绝原目录静默更换manifest。汇总器保留scope/limitations，不再强写“独立”来源。15项小测试通过。
- 第三轮中重档暂不再生成。只调整轻档至面积.30、位移.12、纵向逆尺度.975、剪切.015、混合.45，拟生成`dev_T2_attempt04_light`单条；这条因目标已知只能先作开发机制检查，后续须与其他候选混合复核，不能以单条知情意见宣称正式四档验收完成。
- 7033已成功退出，第三轮4条完成；资产回传57449成功。新工具`audit_v3_candidates.py`完成一次轻量包依赖hash、manifest内容绑定、当前16Patch布局hash及可见标签schema核验，结果4/4通过，外置盘`audits/A_T2_attempt03_received.json`。不重复此不变内容审计；它不重算decoded几何或代替物理/视觉验收。
- 此工具拒绝过期布局标签、重复ID/hash、越界路径和已有审计覆盖。10项小夹具生成测试通过（含有效审计及过期布局标签拒绝），临时文件在外置盘`_scratch/pytest_received_audit_20260906_0945`。
- GPU证据会话58203成功退出，第三轮全部4条512与1024七视角均完成，输出`previews/A_T2_attempt03_blind`和`previews/A_T2_attempt03_detail_v2`；已启动两个目录回传。下一步同步结束后，先查看public证据记录独立意见，再查private_mapping，当前未看第三轮对应关系和图像，不按参数猜分。
- 第三轮四个匿名ID依次c8d36531dea02419a783、232f84382a27b16b250f、7fa8a0c02d3d9b2ac375、13abbc4c065f2cc2feba。知道这是T2批次，不能宣称类别盲；目标/参数隐藏评分。旧评分、失败版本与准入门槛保留。
- 当前正式准入仍0、无新训练，没有等待中的生成作业。下一轮以第三轮视觉结果决定是否需要第4轮，同时逐步补A原33条细节复核和其他开发Clean，不重启旧7033/58203。

### 最新接续：2026-09-06 09:21起，T2细节复核与第三轮有界投影候选

- GPU已快进`441d483`，本地exec会话**7033**运行A四条T2第三轮，输出`assets/B415722108801063A0/dev_T2_attempt03`；使用已同步的`ratings/clean_admission_visual_v2/B415722108801063A0.json`。下一轮核对7033/进程/4条manifest，不能重复启动或覆盖目录。旧33版本和T2第二轮未修改。
- 外置盘已挂载，约822GiB。第二轮T2四条逐条看原生view0和七视角拼图；先另存`ratings/A_T2_attempt02_detail_v2/supplemental_opinions.json`，再读映射。新意见按level1→4为5/5/3/3，仍0/4目标匹配；高两档从旧512意见4改为3，理由是原生细节下多条立面窗格的叠影更明确。旧意见保留，本次此前已见过样本，不称新独立盲评，也不是正式标签。
- 仅平移＋固定半混合对重复窗格立面影响受限；新候选保持同UV域采样，加入有界逆仿射纵向尺度、剪切和线性光混合（`raster_connected_inverse_affine_v3`）。用于近似投影/UV参数化形变，不宣称来自真实相机参数。旧函数默认行为保留，边界仍为同连通域最近有效像素，碰触UV岛合并及边界延拓局限继续记录。
- 第三轮T2四目标起始配方：面积.45/.65/.85/.97、位移.18/.25/.35/.45、纵向逆尺度.90/.65/.35/.12、剪切.04/.12/.25/.40、混合.5/.7/.9/1。均为开发候选，不保证观测档位；最多6轮上限不变，不将参数当标签。
- 本地24项小夹具测试通过，临时数据在外置盘_scratch/pytest_affine_20260906_0925。测试含重复条纹被形变、alpha/非支撑区不变、断开UV域不串色、参数越界拒绝，以及组合候选；未运行本地训练或正式生成。
- 下一步GPU只生成A的T2四条第三revision，不重生成原33条或第二轮资产；结束后同步并生成两规格证据，再补充复核。A原33条1024补充意见仍未做完；三栋开发校准和正式数据尚未通过。

### 最新接续：2026-09-06 08:59起，V3联合质量头小测试通过

- 本轮收尾：GitHub与GPU隔离代码均到`6c52bc3`（日志后续提交另算），历史fixture本地改动保留。GPU首次测试25通过、9项因新_scratch父目录不存在而setup失败；创建该明确目录后重跑候选模块10通过，共34个不同测试通过，不是模型/数据退化失败。当前没有生成/训练作业运行，由心跳继续，不要等待旧20229。
- 查看C5第二轮细节总览及原生view2，暂定2未变，T2仍因暗部掩盖而不确定。另存外置盘`ratings/A_C5_attempt02/development_observation_detail_v2.json`，绑定内容和receipt hash，明确目标知情开发意见、非MOS、非正式标签；没有将target1强塞成观测1。其余33＋4条细节尚待复核。
- 外置盘真挂载，约822GiB空闲。46908、75063均成功退出；T2第二轮4条与C5第二轮1条1024细节均已生成，C5同步55575也成功退出。补充证据目录`A_dev33_detail_v2`、`A_T2_attempt02_detail_v2`、`A_C5_attempt02_detail_v2`均已在外置盘；无需再渲染这些相同证据，下一步是补充视觉复核而非生成。
- 新建包内`v3_quality_head.py`：轻量共享Patch编码/掩蔽均值聚合，建筑和16Patch分别输出3质量接口与6独立异常logits，额外有单调整序的5档ordinal接口。forward只接受当前资产特征与Patch有效mask，没有Clean/攻击/目标档输入。它是首层基线模块，不替代计划中的GATv2/层次模型比较，不是已训练模型。
- 损失按建筑/局部、质量/属性分别使用valid mask；总体档位不复制给局部或几何纹理分项。无效Patch的NaN先屏蔽再编码，空监督可微零；质量顺序显式geometry/texture/overall。Checkpoint同时校验模型schema、6类顺序、16Patch、特征签名，旧发布checkpoint不能静默加载。
- 新头与六属性契约**21项定向小测试通过**，包含Patch重排等变、全空监督、两属性同时有效、未知局部分数无梯度、checkpoint往返与旧协议拒绝。未做本地训练。还缺统一标签导出/特征加载器、训练CLI、分级有效能力声明、Val阈值与推理报告，不能说V3端到端已经上线。
- 准入依旧0，新训练未启动。下一轮优先对已齐全证据做补充复核及有限配方调整，同时推进UV复用回投和结构约束16Patch；不同形态开发Clean仍需补齐，不据旧Test/Blind结果筛选。

### 最新接续：2026-09-06 08:36起，六属性监督契约接入生成器

- 补充：代码`6a7ac5e`已推送GitHub；89942随后成功退出，T2四份细节证据齐全，已启动回传；并启动`previews/A_C5_attempt02_detail_v2`单条同规格补图。下次先检查这两个目录同步/receipt状态，不重复运行。
- A原33版本1024细节证据已完成：远端33份receipt、无该渲染进程；回传会话48627成功退出。外置盘`previews/A_dev33_detail_v2`已同步，不要重生成。尚未据此更新评分。
- 新增`v3_attribute_labels.py`独立版本schema：建筑6属性、当前Mesh固定16×6 Patch属性，未知存null；有效正例/负例必须有明确二值判断、来源、证据、协议与非不确定状态。干预类型不允许当视觉标签；旧softmax类别/order及过期资产/布局digest拒绝。
- masked sigmoid BCE只取有效元素，支持同一Patch多个异常；全未知返回可反传零，不让未知NaN参与损失。已接入候选生成器输出`visible_attribute_labels.json`，初始全部未知，绑定实际current_patch_layout文件SHA256；不回写旧候选、不修改旧发布模型。
- 本地仅小夹具测试，外置盘临时目录`_scratch/pytest_attribute_20260906_0845`：**28 passed**（协议、标签、9种夹具候选），没有本地训练。新接口尚未接入完整V3训练/推理入口，不能称模型多标签功能已上线；证据字段目前为引用，正式导出仍需核验证据文件哈希及真实审核状态。
- GPU会话**89942**正在补T2第二轮全部4个版本1024七视角，目录`previews/A_T2_attempt02_detail_v2`，沿用GPU5a0db36渲染器；完成后同步外置盘，再补C5第二轮相同细节规格。新代码本轮先提交GitHub，GPU尚未更新，后续用增量bundle同步，保留历史脏工作区。
- 接续：完成补充细节复核并保留旧意见；推进新6属性多任务训练/推理接口与UV复用面回投，不把生成干预直接冒充可见异常；筛齐不同形态开发Clean后进行12栋Train试生产。正式准入仍0，训练未开始，Test/Blind保持锁定。

### 最新接续：2026-09-06 08:12起，A维持5，全部33版本细节证据补齐中

- 62105成功完成，51554全部同步。查看商业楼与A七视角，另查商业楼view2/view3和A原生view0。商业楼背面上部有明显三角扭曲/映射异常，不准入；广告字样不完整原因未知，不据此单独下结论。决定另存`ratings/clean_material_v2/B367481578701063A0_decision.json`。
- A主窗格/屋顶/轮廓清楚，独立机器意见维持5；纯色基座仍记录为局限，不能仅凭纯色判缺纹理。新内容与七视角证据哈希绑定记录`ratings/clean_admission_visual_v2/B415722108801063A0.json`，不覆盖旧版本。源包hash匹配历史qualified审计。
- 新通用记录器`scripts/record_v3_evidence_review.py`接受明确人工/AI给出的等级与理由（当前来源写Codex_AI），核验七图＋拼图哈希、源digest、唯一技术审计匹配，已有输出拒绝覆盖；本次已实际运行A成功。它不会从图像自动算分，也不把技术合格当质量5。后续需给失配/重复审计等补单测；证据路径当前绝对路径，正式导出时需统一数据根相对路径。
- **当前会话13896在GPU运行**：不重新生成资产，只对A原`dev33_attempt01`全部33版本顺序补原生1024px七视角到`previews/A_dev33_detail_v2`，新证据版本不覆盖512px。已有匿名ID与旧批次一样，因此复核者可能记得旧样本；不得宣称完全独立新盲评，可记“隐藏目标的补充细节复核”，旧评分保留。
- 下一轮先查13896/receipt进度，未完成不重复启动；完成后同步该新证据目录，统一记录补充意见与变化理由。T2第二轮/C5第二轮也需要同规格细节，之后混合复核，不能只对差评样本补图。
- Clean低命中率不是训练结果问题；不根据Test/Blind放宽标准。当前仍缺不同形态开发锚点，可继续改进候选预筛；更应推进不依赖名单的P0新6属性标签schema、unknown mask、单输入推理协议，避免整轮只在筛楼。正式准入0，训练未开始。

### 最新接续：2026-09-06 07:54起，第二批细查结束，商业楼与A统一细节证据运行中

- 45566成功退出，两栋全部证据已同步（14244结束）。查看七视角及原生单图：B359061766501063A0主立面明显发糊；B360731545401063A0侧面出现不自然屋面/侧面投影拉伸外观，均不准入scale5。决定保存`ratings/clean_material_v2/batch02_followup_decisions.json`。第二批12栋仍无新scale5锚点，不能宣称已完成三栋校准。
- 当前**会话62105**顺序渲染商业楼候选B367481578701063A0（11-SW-10C，此前仅旧低清视图）与首栋A B415722108801063A0的七视角1024px新证据，输出`previews/clean_material_v2/<ID>`。A也按相同标准复核，不因已有机器5就豁免；其已有512px与旧896px意见保留。
- 下一轮先检查62105成功/receipt并同步这两个新目录，再查看总览与原生细节。如果A也达不到细节协议要求，记录版本化结论，不继续把它强行当锚点。不能把仅技术qualified的原模型当scale5；当前随机初筛命中率低，后续可在不使用下游质量模型分数的前提下改用几何/纹理完整性预筛排序，提高查阅效率。
- 当前未开始训练、正式准入仍0。其余不依赖Clean名单的标签schema/无参考链路适配可继续推进，避免所有工作只等待筛选；旧结果和Test/Blind不动。

### 最新接续：2026-09-06 07:35起，第二批12栋初筛运行中

**本轮末已完成：41073成功退出12栋，23502回传成功。逐栋查看相对两侧view0/view1，10栋已有明显灰区/模糊等不选作锚点；B359061766501063A0、B360731545401063A0待完整复核（初筛记录在`ratings/discovery_train_batch02/screening.json`，未单独看顶视，不伪称三图都已看）。当前新会话45566正在顺序生成这两栋七视角1024px到`previews/clean_material_v2/<ID>`。下一轮检查45566并同步这两个目录；不要再等41073或重复初筛。**

- 62109已成功完成并同步B353361775001063A0证据；查看七视角和原生view2后不准入scale5：附属体黑灰区较大、局部底边不完整。真实玻璃反射不视为缺陷。决定保存`ratings/clean_material_v2/B353361775001063A0_decision.json`，没有强行赋全局低分。第一批8栋最终无新scale5锚点。
- 已排除原13个检查ID与batch01全部8个ID，固定seed2026从剩余GPU现有历史Train中取12栋，**当前会话41073**，输出`previews/discovery_train_batch02`。三方向512px，只初筛，不生成退化、不自动准入；不会重复batch01。
- 下一轮检查41073/12个receipt，完成后同步该新目录，逐栋看三视角，值得细查才补七视角。原候选A仍为开发锚点，B为相似住宅备用；需补不同形态，不为凑数量放宽标准。数据正式准入0、训练未启动；Test/Blind保持未读取。

### 最新接续：2026-09-06 07:14起，切换8栋有限初筛

**当前在跑会话62109：仅B353361775001063A0七视角1024px复核。**

**本轮末：85537成功完成8栋且已全部回传，已逐栋看三视角。7栋因大片灰区/拉伸或建筑片段不适合锚点；B353361775001063A0列入七视角待复核（底部疑似缺口、附属体灰区仍需确认），不是scale5。筛选决定在`ratings/discovery_train_batch01/screening.json`。已启动该唯一候选1024px七视角复核，下一轮检查新会话并同步`previews/clean_material_v2/B353361775001063A0`。不要重复8栋初筛。**

- 办公楼28527成功退出，证据已同步。已查看宽体B358611580502063A1及办公楼B358311783201063A0七视角：前者侧墙/屋顶大片灰区，后者多个立面模糊拉伸，不作为scale5锚点；决定另存`ratings/clean_material_v2/screening_decisions_second_pair.json`。不再等待旧渲染/重复这两栋。
- 为减少无效高分辨率筛选，新增`scripts/screen_v3_clean_batch.py`：只在历史qualified Train、GPU现有源包、排除已检查ID后seed2026打乱，先固定8栋名单再512px三方向初筛（方向索引5/6/4）。最多12栋/批；全部只是候选，rating=null，不能据技术qualified直接叫质量合格。入围后才补齐七视角1024px。
- **GPU运行会话85537**，代码5a0db36，输出`previews/discovery_train_batch01`，`screening_plan.json`先于渲染保存，逐栋receipt完成标记。没有生成退化/训练。不要重复启动同目录。
- 下一轮检查85537退出和receipt数量；只同步新batch到外置盘同构目录，逐栋看三视角（不能只看缩小的8栋总图），筛出有希望的补七视角。若因源包缺依赖失败，记录具体ID并修复该项，不重新跑已完成图像。此工具目前已有目录拒绝覆盖，续跑需增加显式receipt校验恢复功能或用新输出目录处理剩余项，不能删掉已完成数据。

### 最新接续：2026-09-06 06:55起，源灰区已定位，下一对候选渲染中

**当前唯一重启渲染会话28527：办公楼B358311783201063A0，改用V3/source中的已同步源包。下一轮检查退出与receipt，然后同步该目录并与已回传宽体证据一起复核。**

**本轮末：57802结束，宽体B358611580502063A1证据完成且已回传外置盘（60120结束），尚未查看新七视角；第二栋因GPU旧源目录缺文件失败。已从外置盘仅同步该办公楼到GPU V3/source/11-SW-4B/BUILDING/B358311783201063A0（38842结束），重新启动单栋渲染，见下一条会话补记。不重跑宽体。**

- 对B361611792502063A0做有限源数据检查：Face921占表面积约5.10%，材质0，三个顶点UV有效；中心与三个内部重心采样点从原生JPG直接得到RGB(61,61,61)。已查看源001.jpg，灰底与模糊区域确实存在。至少该面灰区不是渲染器凭空产生；不能据有限采样声称所有灰区原因都查清。诊断保存在`ratings/clean_material_v2/gray_source_diagnosis.json`，不再重复相同渲染。
- 复用已归档的12号候选总览寻找形态不同的备选，没有自动赋分。挑选宽体B358611580502063A1（11-SW-9D，约216×112×59）及屋顶复杂的办公楼B358311783201063A0（11-SW-4B，约32×30×40）进行原生1024px七视角材料感知复核；历史均为Train，不接触锁定评测数据。
- **当前GPU链本地会话57802：** 顺序运行上述两栋的`render_v3_clean_evidence.py`；输出`previews/clean_material_v2/<asset_id>`。代码沿用已部署a5624fc，新目录拒绝覆盖。未生成退化、未评分准入。
- 下一轮先检查57802是否完成，rsync仅上述两个新目录回外置盘，然后查看七视角总览＋必要原生单图。不要只凭旧低清总览断言合格。如果合格，再绑定当前源/证据digest写开发准入；不合格继续保留拒绝记录。当前两个低层旧候选不作为锚点，正式数据0、训练未启动。

### 最新接续：2026-09-06 06:37起，低层Clean材质感知证据生成中

**本轮末完成：31869退出成功，两栋全部证据已回传（69760完成），已查看七视角总览。两者仍有大范围屋顶/侧墙灰区及局部发糊，不准入scale5锚点，未强行赋低分，因为尚未完全排除采样/材质处理原因。决定记录`ratings/clean_material_v2/screening_decisions.json`。下一轮不要再渲染这两栋相同设置；优先筛其他候选，或对灰区做源UV/纹理与独立渲染诊断。当前无此渲染任务在跑。**

- C5第二轮资产已完整同步，包digest核验通过；不用再等待88045。上轮开发观察保留，不是正式盲评标签。
- 新增`docs/V3_VISUAL_EVIDENCE_PROTOCOL_ZH.md`并链接主方案：全景512px＋同七方向原生1024px细节；同一开发建筑全部版本一致适用，不能只放大失败样本凑分。1024px是同视角更高采样密度，不冒充真正相机近景；旧评分保留，新证据另存。现有候选尚未全部补齐，不宣称完成v2评分。
- 新增`scripts/render_v3_clean_evidence.py`用于GPU有限Clean筛选，记录内容/图像digest、材质感知版本和统计，quality_rating=null；输出已存在则拒绝覆盖。语法编译通过，复用既有已测试的材质感知渲染入口，不新建网站。
- GPU代码a5624fc。**正在运行本地exec会话31869**：顺序渲染B359921765302063A0、B361611792502063A0两个Train历史候选（11-SW-5A，2558/1873faces）原生1024px七视图；输出`previews/clean_material_v2/<asset_id>`。过去legacy预览屋顶大片灰色，尚不能确定是否纹理或渲染问题，本次用于澄清，不自动准入。
- 下次先检查31869退出/receipt；只同步已完成证据到外置盘同构目录，逐栋查看总览与原生view。若确有缺陷则记拒绝，不为了补足三栋名额放宽scale5。尚需两个不同形态高质量开发Clean、统一细节评分以及受控生成参数修正，正式数据仍0、训练未启动。

### 最新接续：2026-09-06 06:16起，T2复核完成；C5曝光修订运行中

**本轮末补充：C5会话16113已成功退出，七视角证据已同步并查看。明显欠曝光但仍有窗格可辨，开发意见scale2；荧光黄绿消失，T2是否被暗部掩盖仍未知，不自动标双异常。目标知情开发观察另存`ratings/A_C5_attempt02/development_observation.json`，不是盲评标签。资产rsync会话88045正在回传，下一轮先检查退出/哈希。下一优先项是统一近景证据规范与另外两栋不同形态Clean，而非马上对同栋继续盲目增强。**

- T2第二轮生成/渲染会话22397成功退出；4条资产与全部证据已同步外置盘，4/4资产包digest一致，证据哈希由评分汇总器核验。不要再等待22397/69873或重复渲染这四条。
- 先看4张匿名总览并保存`ratings/A_T2_attempt02_blind/independent_scores.json`，再打开目标映射：level1→4实际为5/5/4/4，0/4目标匹配。已知批次为T2，故只隐藏目标映射，不声称类型也盲。代码修复解决了支撑消失的机制，但**尚未解决感知档位覆盖**；不能把4强行写成1。
- 当前50%原图＋50%错位图、近邻边界延拓及局部覆盖可能使异常上限偏轻。后续应在开发集受控比较混合比例/UV形变等同类机制；也要考虑512px全景对纹理误差不敏感的评分局限，先按统一视距规则补充近景复核，不能为凑目标临时只放大失败样本。不要马上全量复制当前T2配方。
- 新有界色差函数`exposure_inconsistency`：线性光曝光-0.5/-1/-2/-3.5EV，温暖通道偏移固定0.12（函数限制±0.2），保留alpha与非目标区域；不再使用大幅不平衡RGB增益。记录局部黑/白像素比例，仍是待视觉校准的曝光失配模拟，不是物理相机重建。
- 本地新测试通过；GPU退化与候选生成22项通过。代码5c9fb7b已同步GPU，旧函数/数据未覆盖。
- **运行中：** 本地exec会话16113，GPU命令仅生成A的`dev_C5_attempt02`一个组合候选，成功后自动渲染`previews/A_C5_attempt02`（七视角512px）。下一轮检查进程和清单，完成后同步两个目录回外置盘并复核色偏真实性。只有一个且目标已知，不能把这次单条复核冒充严格匿名评分；记录曝光机制检查与带目标知情的开发意见，正式标签仍需更混合的匿名复核。
- 正式准入仍0；未开始训练。接下来补充统一近景证据规范、剩余两栋多样性Clean筛选，并完善受控配方参数覆盖/最多6次尝试记录，避免无记录反复调参。C5任务不是全套生成完成。

### 最新接续：2026-09-06 05:57起，T2第二次候选正在GPU生成

- 已修正T2采样：目的地损伤mask不再与平移mask相交；源采样限制在有效且非透明的同一栅格连通UV域，超出域的源点投影到该域最近有效像素，不跨分离岛/透明背景取色。位移按UV域包围盒短边归一化，四档起始比例0.08/0.18/0.32/0.48；仍是待校准参数。
- 局限明确：接触的UV岛可能在栅格域合并；边界延拓是近似，不是原摄影相机位姿驱动的真实重投影。不能由该修复保证四档观感单调或全部到位，必须独立视觉复核。
- 新记录`pixel_changed_op*_material*`实际RGB变化mask，与原干预支撑mask分开，后续UV复用回投仍待实现。alpha保留、非目标像素不改变。旧`local_texture`保留给历史调用，不覆盖旧数据。
- 本地3项定向测试通过；GPU相关退化/候选生成/评分绑定24项全部通过。代码`1fceb43`已推送GitHub并以同提交bundle快进同步GPU。远端fixture的两个历史测试文件改动保留，不强行覆盖。
- **正在运行：** GPU生成PID775492（父shell775491），本地exec会话22397。仅A的T2四槽第二次尝试；输出`assets/B415722108801063A0/dev_T2_attempt02`。生成成功后同一命令自动接着渲染七视角512px匿名证据到`previews/A_T2_attempt02_blind`。不要重复启动此链，也不要覆盖attempt01/既有评分。
- **下一轮：** 先检查22397或远端进程、candidate_manifest四条和public queue四条；完成后rsync新revision与证据到外置盘同构目录。只看public证据给4条独立意见，先保存再查看private_mapping。未完成前不声称第二轮成功；无需重做首轮33条评分/审计。随后处理有界曝光/色温机制及其他未匹配槽位；当前C5极端黄绿问题尚未改。

### 最新接续：2026-09-06，首栋33候选匿名评分已锁定

- GPU生成和匿名七视角证据均已结束，完整同步外置盘；不再等待旧PID770065/会话36786，也不要重复渲染。新资产哈希校验此前已完成33/33。
- 本次先逐一查看33张匿名七视角总览，将33条AI意见写入`ratings/A_dev33_blind_v1/independent_scores.json`，之后才打开private_mapping。这是机器视觉意见，不是人工MOS；512px固定视角有分辨率/遮挡局限，不声称完整交互检查。
- 新`summarize_v3_blind_review.py`核验全部264张图像哈希、完整ID集合、内容绑定，再对照目标；不覆盖评分、不自动准入。报告在同目录`diagnostic_report.json`。3项测试通过（保留原意见、证据改动拒绝、重复ID拒绝）。
- 33条中19条目标匹配（含Clean）；正式准入仍0。单类level1→4实际AI等级：G1=5/3/2/1，G2=5/5/2/2，G3=4/3/2/1，T1=5/3/1/1，T2=5/5/4/5，T3=5/3/2/1。Clean独立评5。组合C1→C8分别1/3/3/5/1/2/3/5。
- 不能据此强行改成目标标签。G3虽四档匹配，但观察到拉伸、缺口等伴随异常，不能只标G3；C5虽匹配1但荧黄色过强，物理真实性待修正，不能因匹配就通过。
- **下一步优先修复T2，再小规模生成新revision：** `local_texture`当前用`active = mask & shifted(mask)`，位移越大，实际生效交集可能越小；窄UV岛导致重度接近无损，是非单调的重要机制嫌疑。应分离“目的地受损区域”和“有效源UV岛采样域”，限制跨岛/背景串采，记录真正改变像素面积。需要针对窄/分离UV岛、越界与alpha保持的测试，不能简单全图roll或盲目加大shift。
- 色差也应限制不合理白平衡增益，采用有界曝光/色温、记录饱和比例与局部范围；当前线性RGB通道不平衡增益在C5产生极端黄绿，不适合作为已校准正式配方。
- G1/T3轻档需略加强；T1轻档需加强，中重之间需要补足scale2；G2低档过弱，高档未到1。先用开发A有限候选校准，不重生成已匹配且物理待确认之外的全部33。要保留每次生成与独立评分，最多6次/槽，不把参数值当评分。
- 仍需两栋形态不同的开发scale5 Clean及12栋Train试生产。未建正式4950条、未开始新训练，Test/Blind未读取。心跳继续，下一轮直接修复上述T2机制，不重做已完成匿名评分和哈希审计。

### 心跳推进：2026-09-06 05:03起，首栋开发生成运行中

**最新检查覆盖下面的“运行中”描述：A生成进程已成功退出，33条候选、33个不同decoded_digest、正式准入仍0。远端资产约1.3GiB。**

**补充完成：rsync会话39581已成功退出；外置盘33/33资产包依赖哈希与生成记录一致，mismatch=0（校验会话72217）。这部分无需再全量重复校验。匿名证据渲染36786仍运行，最后检查20/33已产出；下一步只需完成/同步证据后评分。**

- 正在回传外置盘同构目录，rsync本地exec会话`39581`，不可在未传完时称为已完整同步。保留远端副本，不删除。
- GPU已启动512px×七视角匿名证据，PID`770065`（先核对命令），本地exec会话`36786`；输出`/root/autodl-tmp/UrbanPhotoMeshQA/data/HK3D-Individualised-V3/previews/A_dev33_blind_v1`。预计33条public证据，先检查进程/queue数量再续跑，已有receipt可校验复用。
- 新证据改用material-aware渲染，尊重baseColorFactor、alpha、sampler wrap；每栋渲染后清空纹理LRU缓存，避免跨样本内存积累。旧历史图像不覆盖，新旧证据有渲染版本区别；独立评分也须复核新证据中的Clean，不强迫其维持5。
- 匿名队列＋缓存复用小夹具测试通过（1 passed）；远端代码`3dfbc6e`。低层备选B35992、B36161历史预览有大片灰区，两个源模型虽有纹理绑定且UV有限，但不能直接据此确认实际高质量，暂不准入，需要材料感知新证据澄清。
- **接续重点：** 等回传完整做一次轻量依赖/哈希校验；同步匿名previews，只读取public队列给33候选独立评分，然后才查private映射统计四档成功率。不要现在训练或继续扩大到150栋。

- 已查看A/B七视角总览，并分别近观A的view0/view3、B的view0。正常观看尺度判断均为机器scale5；记录局限为离线多视角，不声称人工MOS或纹理完美。
- 当前源包digest与iteration2历史审计精确一致，旧视觉证据可复用；新准入记录绑定源digest、证据文件SHA256与本次理由，位于外置盘`ratings/clean_admission_20260906`，脚本`record_v3_clean_review.py`已执行，不重复覆盖。
- A（B415722108801063A0）进入开发校准；B为reviewed_reserve_train_only，与A形态相似，暂不占第二个多样性开发名额。原B35520仍不作为scale5。还需低/中层及复杂形态校准建筑。
- GPU已启动A的33候选生成，PID **769248**（复查命令后再操作），本地exec会话 **75700**。远端输出`/root/autodl-tmp/UrbanPhotoMeshQA/data/HK3D-Individualised-V3/assets/B415722108801063A0/dev33_attempt01`。目前不是正式33条，必须等candidate_manifest条数、进程退出与唯一性检查确认。不要重复启动/覆盖此目录。
- 新增`render_v3_blind_evidence.py`，只离线顺序渲染；public队列为匿名编号＋无文字视图，target/class对应放private_mapping，不给评分环节读取。支持哈希校验后复用已完成图片，尚未运行首栋渲染。
- 代码已同步到GPU **614eddc**。SSH短连接偶发被关闭，重建ControlMaster=auto、ControlPersist=3600、ServerAliveInterval=20后恢复，所有SSH/rsync共享同一ControlPath。
- 下次步骤：检查A生成退出与33条完整性→将完成资产rsync回外置盘`assets/B415722108801063A0/dev33_attempt01`→GPU离线匿名证据生成（512px、七视角、顺序）→同步previews→只读public证据独立分档。不读private_mapping猜分，不把起始参数作为完成一一对应的证明。此后再做各槽最多6次有限校准搜索。

### 心跳推进：2026-09-06 04:40—04:55

- 新入口`scripts/build_v3_33_candidates.py`：按33槽位调度6类与8组合；同栋同类四档机制稳定；T3子机制跨建筑轮换，C1/C8固定缺纹理、C5固定色彩。
- 几何形变以建筑包围盒对角线归一化；局部操作共享3个分散区域预算。删除延后执行以保留组合形变的源面索引，纹理支撑剔除已删除面；QEM映射明确unknown，未伪造精确对应。
- 保存源Face×6干预、区域ID、顶点位移、操作级纹理像素掩码、独立待测16Patch布局。像素掩码还需回投到全部UV复用面，当前可见异常与局部质量有效mask全false，不冒充标签。
- 强制内容绑定scale5 Clean准入，生成记录ratings为空、formal=false；起始参数仍未视觉校准。无16个有效面的重度候选拒绝。
- 本地14项定向测试通过，包含一个小型fixture导出和“不自动生成质量真值”检查。仅测试夹具，不是正式建筑样本；测试数据在外置盘_scratch。
- GPU新建隔离环境`/root/autodl-tmp/UrbanPhotoMeshQA/iteration3-env`（共享旧环境只读包），安装pymeshlab2025.7.post1、DracoPy2.0.0。未修改其他项目环境；旧环境确实缺这两个包。
- 下一步先验证新入口在GPU的小fixture与组合操作；完成3栋Clean准入及独立盲评分队列。新入口暂未实现6次候选搜索/单槽失败恢复、正式满额验收、UV复用全量回投和可见标签确认，不能批量冒进4950条。
- 本地额外完成7种组合的小夹具实际生成，9项测试通过；正在补齐C4覆盖。GPU直接GitHub拉取出现网络停滞，停止本任务的git-remote进程，改用同一Git提交的增量bundle传输，不覆盖历史工作区。
- 最终GPU运行全部8种组合及T1小夹具、新协议与基础退化测试：**22 passed in 20.38s**。远端V3代码已快进到`eed51db`；小fixture实际二进制通过受控rsync补齐LFS跳过的内容。未运行正式建筑生成或训练。接续优先Clean准入/无目标提示的评分证据清单，而非再跑这些不变测试。

1. 完成6类生成器（不能沿用旧50版本宣称完成），多区域预算与逐Face/UV证据。
2. 原目录仅10个历史已接触图幅；全新Blind图幅资源尚未补充，不能把旧图幅更名为新Blind。
3. 3栋开发Clean独立复核；原第一栋已有scale4意见，不入scale5锚点。新A/B仍需内容绑定准入，补充第三栋及建筑形态多样性。
4. 开发校准通过后12栋Train试生产，冻结标准后再正式150栋。当前正式准入0条，没有开始新训练。
5. 继续P0标签/多标签/缓存协议修复；新schema与旧release隔离，保持旧结果不变。

停止条件是协议通过、有效数据齐全、Val完成预注册比较并冻结，以及最终一次评测完成；不得为“满意”而根据Test/Blind重新调参。


## 2026-09-06：真实原始样本分级保留与训练补充建议（接续记录）

- 用户要求非 scale5 原始建筑也独立分级并保留，已建立外置盘 `ratings/raw_quality_backup_v1/`，原始资产和已有证据原位保留，不重复生成、不删除。
- 当前索引共61栋：AI暂评4级14栋、3级27栋、2级18栋、1级1栋、证据不足未定级1栋。包括复用先前14栋详细复核中10栋非5意见，以及第二批原始建筑前51栋三视角拼图意见。拼图意见均标记不确定，不等同严格七视角准入或人工MOS；六类/局部质量不能由整体分数推导。另有133栋历史记录列入补评队列，该队列待逐项复用证据评分，不表示已完成分级。
- 新批240栋技术检查完成，138栋达到当前预筛技术条件，138栋七视角原生1024证据全部就绪，共46张三建筑拼图。已检查并记录前17张（51栋）；第18张开始尚待检查。记录脚本在外置盘 `jobs/record_raw_backup_opinions.py`。
- 开发补缺批首次26次尝试实际24成功、2参数边界失败；C的T2和B的T3均以独立fix1目录修复成功且512/1024证据就绪，失败目录保留。三个开发建筑现共136个成功候选版本（原110+26），不计失败尝试；正式验收仍0、新训练尚未开始。B的T3补跑匿名ID `17724ab7d2f9249107dd`，C的T2补跑匿名ID `8ac21e17e654f9ce46c6`。新增候选仍待混合复核。
- 用户进一步询问是否让真实1–4级原始模型参与训练。建议增补独立的真实原始样本训练池，以受控合成集加真实原始集进行Val消融，保持现有150栋/4950条主集目标、3开发与12试生产验收以及Test/Blind锁定；此处为方案建议，尚未自动将备份设为训练可用或改写正式准入协议。

## 2026-09-06：用户批准真实原始训练池增补

用户明确批准上一条建议。已将现行方案版本更新为V3-plan-6class-33-real-v1：保留受控主集和原验收顺序，新增真实1–5级原始样本池，质量/六属性/16Patch各自按可信监督准入，不能自动启用备份分数。Test/Blind锁定不变。
原始目录实查2485栋独立Building、10图幅，排除AppleDouble伪文件；当前Train范围排除历史及两批480栋后尚余1622栋，不需要用户现在额外下载。容量记录见外置盘audits/source_capacity_20260906.json。数量不代表合格scale5数量。

## 2026-09-06：迭代三统一归集与真实样本继续复核

- 用户再次指定全部迭代三数据归集到外置盘HK3D-Individualised-V3。已建立根目录README_DATA_ZH.md导航；raw_original/图幅/建筑ID完整复制67栋原始包，逐包内容哈希验证67成功0失败，源目录保留。candidate_manifest的dataset_gltf均为该根目录下相对路径。
- 分级备份67栋（4级17、3级27、2级20、1级1、未知2），包含第二批原始拼图前19张/57栋。详细复核3栋：B357081764802063A0=2、B365861558402063A0=2、B359581527201063A0=3，均有七视角总览和明确原生细节图；只标确认的T2存在，其余异常/局部未知掩蔽。保留旧初筛意见，不冒充MOS。
- 完整包归集不是训练准入。67栋候选当前训练准入0；固定16Patch预备任务基于现有拓扑实现，结构约束验收仍待完成；受控3栋136候选正式验收0，新正式训练未开始。
- 首批61栋GPU传输正在进行，随后运行有界Patch预备；服务器的61栋清单为本批快照，后增6栋留待下一批。不要在首批完成前覆盖其清单或重复启动。

## 2026-09-06：持续接续恢复与Mac就地数据准备

- 用户明确要求持续执行、4小时推进窗口，并授权充分利用Mac CPU/内存、关闭无关进程。已将原心跳urbanphotomeshqa-3迁移至当前任务01a07481-8678-7061-875c-3c4cd0951389并设ACTIVE，保留15分钟频率，没有创建重复自动化；旧任务不再是目标。
- 资源实查8核/16GiB、约63%内存空闲；仅关闭Activity Monitor(PID80717)，保留Codex、系统服务和网络代理。后续CPU筛选/整理/标注/16Patch就地使用Mac外置盘，避免为CPU步骤往返GPU服务器；真正GPU特征与训练留RTX4090。已经完成的服务器结果复用。
- 第二批138栋七视角证据的46张拼图全部初筛完成。分级备份累计148栋（原详细10＋本批138），暂评4级40、3级56、2级45、1级1、未知6；均不是正式验收数量。
- 148栋完整原始包现已在raw_original内哈希验证复制成功，0失败。16Patch：服务器原61栋全部16有效，回收复用；Mac4进程补齐87栋，0失败，累计每项处理时间15.68秒（不是整批端到端耗时）。148栋均有16有效Patch，结构约束验收仍未完成，全部局部质量未知。
- 详细意见已有6栋，其中B365201779201063A0=4不确定；B359491558101063A0=5、B367231577201063A0=4，均新建版本保留原初筛；后者明确可见T2，其他未知属性掩蔽。B359491558101063A0纳入AI5 Train备用池，正式划分与准入未启用。详细6栋中可解析可信整体意见5栋，训练准入0。
- 外置盘jobs/refresh_real_original_pool.py、organize_real_original_pool.py、prepare_real_original_patches_mac.py、record_raw_detailed.py可恢复；禁止把初筛全自动设为训练有效。当前正式数据0、GPU正式训练未开始。

## 2026-09-06：Mac批量复核与真实覆盖核实（本任务继续执行）

- 原始148栋资产归集及16Patch准备保持完成、失败0；详细分级现在18栋（17栋非不确定意见），意见路径ratings/raw_quality_detailed_v1。新增AI5备用B359491558101063A0、B364811507801063A0；Train备用现8栋，尚未满足12栋，不能计为试生产合格。candidate_manifest已刷新实际详细意见，不启用未审标签。
- 26个开发修订候选均已复用七视角512总览和原生1024细节记录AI诊断意见，未重渲染。neutral_queue_v2补入最后B修复项；该项明确目标知情。全部意见保留既往开发上下文局限，非独立人工MOS。汇总audits/dev_gap02_mixed_diagnostic_v1.json：20个目标分匹配，5个不匹配，1个灰面证据待材质核实。分数匹配不等于类别、真实性及覆盖验收。
- B的G3四档虽然分数4/3/2/1，主要可见异常却是G2形态折叠，不能以生成名字贴G3。C的G2修订实际5/4/3/1，缺目标2；B的G1轻档实际3，T3最重实际2。需要机制/槽位处理，不应据此进入4950条批量。
- Mac4进程完成12个G2/G3修订的表面积位移审计，audits/gap_geometry_coverage_v1。B导出压缩掉未引用顶点，首次8个索引断言失败记录保留；改用面角点对应并与保存位移逐项校验后8个成功，C原4个复用。六个位移阈值及高度/法向分组仅描述几何覆盖，不是感知真值或语义分割。在对角线0.1%阈值下，B G2四档受影响面积52.8/72.7/84.8/90.4%，C为13.2/39.6/68.8/85.1%；说明不能仅把屋顶边缘视觉突出解释成墙面完全未覆盖。
- Mac4并行完成本地11个纹理修订的UV复用面积回投，audits/gap_uv_support_v1；生成支撑与可见标签继续分开。B最后T3修复资产正在从GPU增量回收，随后只补该项，不重复已有99原版本审计。
- RTX4090实际检查利用率0%、显存0，无旧作业并发；新正式训练尚未开始。当前正式验收仍0，受控开发仍3栋136候选。计算资源不是当前主瓶颈，类别有效性和可信监督还未通过。

### 本轮后续：G1面积预算修复与3个新候选

- 原始详细分级32栋，其中3栋仍不确定；新增AI5 B361631540401063A0、B362171559501063A0，Train AI5备用累计10栋。原始分级备份148和16Patch148不变。
- 已修复G1部分面被整面删除造成的预算超调：新增fractional_nested_triangle_holes_v1，按面积切除嵌套中央缺口，剩余面UV插值，并保存current_face_source_index、current_corner_source_barycentric、source_face_retained_fraction。导出后检查面角点对应；不产生自动质量或可见标签。
- Mac小夹具6项验证通过，包含面积/UV/绕序/来源、G1、C1及G1+G2的C6/C8。测试临时文件全部在外置盘_scratch。Mac随后3进程生成B的3个轻档搜索候选，资产及512/1024证据全部完成，失败0，目录assets/B414372125401063A0/dev_g1_fractional03_{a,b,c}与previews/g1_fractional_trial03。
- 三候选实际删除面积1.5/3.5/7.5%，本轮目标知情AI意见5/4/3，记录ratings/g1_fractional_trial03。中间候选dd9c5dfc2bbe31100ec8可作为轻档待混合复核项，不计正式合格。全部受控候选现139=原136+3，开发建筑仍3栋。
- 末项B T3修复资产已同步且UV回投补齐，纹理修订回投共12项完成。C灰面材质检查audits/gap02_gray_material_check_v1.json发现实际纹理包含大块RGB128，不是简单缺图报错；未自动解除该意见的不确定标记。
