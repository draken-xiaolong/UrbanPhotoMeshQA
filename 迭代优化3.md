# UrbanPhotoMeshQA 迭代优化3：六类城市摄影测量 Mesh 质量数据集方案

> 当前建议版本：V3-plan-6class-33，2026-09-06。  
> 用户已于2026-09-06授权按本方案执行：旧数据仅归档、不直接删除；先建数据，再GPU实验。不得启动或渲染示例页面。  
> 固定随机种子2026；无参考推理；Val选模；Test/Blind锁定。  
> **本文“当前方案”优先于末尾历史记录，以及评分规范中旧的600/7500条、10类50版本、纯人工训练等规模与流程描述。** `docs/V3_人工质量评分规范_ZH.md`中五档观感定义可继续参考，其规模、类别与标签来源条款须按本方案同步后才能冻结。

## A. Review结论与本轮决策

推荐采用 **120栋主集＋30栋跨图幅Blind，每栋33个版本，总计4950个样本**。其中每栋为 **1个合格原模型＋6大类×4个质量目标档＋8个跨类组合**。这是规模与覆盖之间的研究设计建议，不是顶刊录用门槛。

相比继续增加同栋强度，优先保证独立建筑、结构、材质和图幅的多样性。用户先参与少量标准校准，后续以生成过程记录和独立AI视觉建议辅助标注；人工有有效评分时优先。**合成退化不等于机器评分，AI评分不等于人工MOS。**

本次review修正了五个风险：

1. 把旧600条纯人工方案与后来的机器辅助方案分开，不再混用。
2. 从“每个生成算法都是一类”改为6个可感知问题大类，子机制保留可追溯信息。
3. 将四档一一对应作为生成后验收门槛，不把目标档位直接写成实际标签。
4. 区分“施加了什么操作”“实际可见什么问题”“总体看起来有多差”，避免按生成名称贴单标签。
5. Patch干预掩码是弱监督，不冒充局部感知质量真值；机器标签上的SRCC也不冒充人类感知一致性。

## B. 对象、规模与划分

研究对象仍为香港摄影测量单体 **Building textured glTF Mesh**，不引入飞机、动物或其他通用物品。应表述为“面向可视化与交付的感知质量”，不声称无参考分数能证明建筑测绘精度或物理尺寸正确。

| Split | 独立建筑数 | 每栋版本 | 样本数 | 用途 |
|---|---:|---:|---:|---|
| Train | 72 | 33 | 2376 | 模型训练 |
| Val | 24 | 33 | 792 | 选模、早停、校准与异常阈值 |
| Test | 24 | 33 | 792 | 最终候选冻结后评测 |
| 主集合计 | 120 | 33 | 3960 | 图幅可交叉，建筑严格隔离 |
| Blind | 30 | 33 | 990 | 主集以外的新图幅，额外计算 |
| 正式总计 | 150 | 33 | 4950 | 150原模型＋3600单类＋1200组合 |

- 3栋开发校准建筑单独管理，不计入150栋，也不进入Test/Blind。合格的3栋按新协议预期共99个版本；临时搜索样本另存，不计正式规模。该安排是本次建议，实际名单尚未冻结。
- 原 `B355201752601063A0`目前建议scale4，不能作为scale5锚点完成一整套四档校准；可保留作低基线诊断样本。新候选A/B仍需通过新标准准入，不能因历史文件名含scale5自动通过。
- 正式150栋不应全是相似高层住宅。覆盖高/中/低层、塔楼与裙楼、复杂屋顶、不同立面和UV布局；优先补足当前试运行缺少的形态。
- Train/Val/Test允许混合图幅；同建筑原模型、退化、所有历史版本和缓存必须同Split，额外检查跨ID重复建筑、重叠实体和重复资产。共享同一建筑复合体的强相关资产按组隔离。
- Blind须来自未用于标准/参数开发的新图幅；旧Blind若已被反复查看诊断，不简单改名当“全新Blind”。明确记录每个图幅的历史接触状态。
- 先确认开发用与保留图幅，再冻结建筑分组；不得根据待训练模型在Test/Blind上的表现换样本。

## C. 六个退化大类与多标签定义

| ID | 大类 | 允许的子机制 | 需要避免的混淆 |
|---|---|---|---|
| G1 | 几何完整性损伤 | 孔洞、局部缺面、观测不足导致结构缺失 | 文件无法打开不是低质量档，而是技术失败 |
| G2 | 几何形态失真 | 空间相关噪声、尖刺、局部扭曲、重建伪影 | 不用任意整体缩放或刚体旋转冒充质量下降 |
| G3 | 几何细节损失 | QEM简化、过度平滑、细小结构消失 | 简化若造成明显裂孔或纹理拉伸，应追加G1/T2标签 |
| T1 | 纹理清晰度损失 | 降采样、模糊、有损纹理编码；允许合理组合 | JPEG不是独立大类，不要求纯JPEG覆盖所有档 |
| T2 | 纹理映射失真 | 投影错位、重影、UV拉伸或畸变 | 曝光色差不算空间错位 |
| T3 | 纹理完整性与色彩异常 | 无效/缺失纹理、曝光或白平衡不一致、辐射接缝 | 两个子组保留独立字段与指标，不能只学会识别灰块 |

上述6类同时作为本轮异常属性接口，但不是互斥分类。T3是为控制初期类别数而做的工程合并，不宣称纹理缺失与色差是同一种物理机制。正式报告必须分别统计 `texture_missing` 与 `radiometric_inconsistency` 子组；若二者表现明显不一致，可在仅基于开发/Val的后续独立版本中考虑拆成第7类，当前不擅自扩类。

**单类生成**指一个主导问题大类，可在该类内部组合机制；不等于强制one-hot实际异常标签。同栋同类四档尽量使用同一机制配方、同一起点，增强损伤程度或范围；不同建筑轮换预先规定的配方。不得让scale4永远是JPEG、scale1永远是降采样，以免模型靠算法身份猜分。

生成时记录：`primary_class`、`recipe_id`、完整操作顺序、每步参数和随机种子。质量/异常标注另存，不把生成目录名、类别、目标档、Clean差值或人工备注喂给模型。

## D. 四个目标质量档的一一对应验收

对每栋合格原模型、每个主导大类，各保留一个 **scale4、scale3、scale2、scale1** 样本。共24个单类退化。页面以“目标scale”优先命名；如果保留level，明确 `level1→scale4、level2→scale3、level3→scale2、level4→scale1`。

验收流程：

1. 从独立确认达到scale5的原模型出发。拒绝以原始身份代替质量准入。
2. 按训练/开发阶段确定的配方和参数区间生成候选；几何参数检查单位和尺度，纹理参数同时考虑分辨率、UV布局与可见范围，不统一套绝对像素值。
3. 评分阶段隐藏目标档、参数和旧机器分，先独立看待测模型，再对不确定样本按固定规则复核。生成选择器可以知道目标，评分器不应直接使用目标决定分数。
4. 记录 `target_scale` 与 `observed_scale`；只有实际独立评分一致、质量内容不同、真实性合格，才接纳到该目标槽位。AI评分记录证据、置信状态和规则版本；人工有效分覆盖机器分，但两者都保留来源。
5. 若不一致，只调整生成参数或同类内合适机制，再独立评；不能手改分数凑齐档位，不能用当前待评QA模型自己的预测作为真值。
6. 建议每个“建筑×类×档”最多6次候选尝试，采用固定顺序的粗到细搜索。达到上限仍失败则标为 `unfillable`，回到开发阶段更换机制或建筑，不扩大无上限搜索，也不把31/32个版本当33个合格版本。
7. 配方/评分规范冻结后，Test/Blind只允许按预先固定的生成与质量控制流程构建，不据下游预测结果调参；人工抽查或标准变更应有版本记录，不静默修改已报告测试集。

“全部对应”是**准入标准，不是当前已经实现的事实，也不是数学保证**。当前样本尚未通过新6类协议。正式Manifest必须列明失败数和各档覆盖率，未满额不能宣称完成4950条。

这会得到一个按质量档均衡设计的合成基准，而非香港建筑自然缺陷频率的随机抽样。论文必须披露按目标筛选的过程，不能由此推断真实部署的质量分布。

## E. 八种组合退化：6种双类＋2种三类

每栋每种组合只保留一个质量目标，合计8个，不再乘四档。组合以真实采集、重建、贴图和轻量化流程中的合理共现为依据，不穷举全部数学组合。

| ID | 组合 | 场景与约束 |
|---|---|---|
| C1 | G1＋T3（缺纹理） | 遮挡或观测不足同时导致缺面、缺纹理；不能把“面不存在”重复当成纹理缺失，纹理异常还应落在可见保留表面 |
| C2 | G2＋T2 | 重建偏差伴随错误投影或重影，缺陷区域允许部分重合 |
| C3 | G3＋T1 | 轻量化发布同时减面和降纹理质量，优先模拟一致的处理链 |
| C4 | T1＋T2 | 贴图模糊与投影重影共存；确认两种问题仍分别可见 |
| C5 | T2＋T3（色彩） | 多视图贴图融合中几何对齐与曝光校正同时不佳 |
| C6 | G1＋G2 | 重建不完整且保留结构出现形变；不能先删掉全部形变区域 |
| C7 | G3＋T1＋T2 | 网格轻量化、贴图压缩与UV/投影误差叠加 |
| C8 | G1＋G2＋T3（缺纹理） | 困难观测区域综合重建失败，三种异常均应在最终资产留下有效证据 |

- 每栋8个组合分配为scale1～4各2个，避免组合全部是极重样本。按建筑顺序和种子2026轮换，**同一个C编号跨建筑覆盖全部四档**。
- Train72栋、Val24栋、Test24栋均能使每个C编号在四档等量；Blind30栋每个C编号各档为7或8个，总体各档仍保持每栋2个组合的配额。
- 不同组合允许重合、部分重合或分离区域，按真实原因决定；单纯要求“同一Patch叠满三种攻击”不合理。
- 后一步操作可能掩盖前一步，必须检查最终有效异常，而不只检查操作日志。不能把单一明显损伤样本硬标成双/三异常。
- 不设Mixed类别；输出多个异常概率。该主协议检验新建筑/新图幅上的组合鲁棒性，**不自动等于“未见组合泛化”**，后者须另做预注册的组合留出实验。

## F. Patch覆盖与局部监督

**当前及后续迭代均固定每栋16个Patch，不增加Patch数量，不采用粗细两套Patch划分。** 优化重点是这16个Patch的空间紧凑性、结构边界、面积均衡和对应可信度。局部几何、纹理与多异常定位共用版本化布局；Patch布局在推理时只能由待评Mesh自身确定，不能依赖Clean。具体替换方案与验收见L节。

### F.1 单个样本的多Patch退化（用户补充要求）

**不仅是不同样本换攻击位置，单个退化样本自身也应优先包含多个有实际损伤的Patch。** 不能长期把同类异常限制在一个Patch，也不能用同一个区域恰好跨越Patch边界来冒充空间多样性。

- 局部机制建议从分散的2～4个有效Patch/空间区域开始分配；这是可校准的生成建议，不是必须达到的感知标签数量。允许一个连续缺陷跨多个Patch，也允许同一种缺陷分布在多个不相邻区域，并记录两种空间模式。
- 轻档可在多个Patch上施加小范围轻微损伤；重档可增大范围或程度，必要时增加区域。总损伤预算由整个样本共享，不能把原单区域强度完整复制到每个Patch，导致最低档立刻变重。
- 同栋同类四个质量目标使用固定且有序的区域候选集合，优先保持位置可比、范围嵌套，再通过生成后独立评分验收。Patch数量不直接决定质量档位。
- 全局机制按实际结果记录多个或全部Patch，不为了满足“多Patch”添加不相关的局部损伤。受可见面、拓扑或真实场景限制的例外必须注明，不伪造受损区域。
- 组合样本记录每一步各自影响的Patch集合；允许同一Patch有多个异常，也允许多个Patch各有不同异常。最终必须检查各异常是否被后续步骤掩盖。
- 统计每类样本的有效受影响Patch数、空间分散程度、受影响面积，以及仍未受损/未知区域的比例。避免所有Patch都被粗糙标红、没有可用于区分的正常区域。

### F.2 可追溯标记与后续兼容

每个样本保存原始Face/顶点/纹理像素粒度证据，并另存聚合的逐Patch记录，至少包括：

```text
asset_digest / patch_layout_version / patch_count
coordinate_frame: source | current
patch_id / region_ids / operation_ids
geometry_affected_fraction / texture_affected_fraction
applied_attributes[6] / visible_attributes[6] / attribute_valid_mask[6]
geometry_displacement_stats / mapping_confidence / label_source
patch_quality_label: optional / quality_valid_mask
```

`patch_id`仅在指定资产内容和布局版本内有效；源模型的Patch3不当然等于退化模型的Patch3。应保留源面到当前面的对应或置信映射，当前布局必须能仅由待测资产重建。面、顶点及纹理像素级证据用于精确记录16个Patch内部的局部受损范围，以及同为16个Patch的布局改进后的标签重聚合，不用于增加Patch数量。布局改变后仍须重新检查映射有效性；仅输出Patch分数的模型不能宣称实现面或像素级预测。

每个Patch分别记录“确实受过干预”“实际可见异常”“是否有可信局部质量分”。已知未干预、未知、被删除和映射失败的区域分别编码，不能统一当正常。未来热力图可按异常类别切换、同时突出多个Patch；生成支撑热力图与模型预测热力图必须明确区分。

本补充目前仅为规范，现有代码尚未完成多区域生成和上述完整逐Patch标签schema，后续执行时需实现并验收。

### F.3 几何与纹理对应的质量控制

- 不同局部退化配方优先使用不同的有效Patch起点，跨建筑轮换朝向、屋顶、立面、底座；同类四档保持可比较的位置并嵌套扩展范围。
- 按表面积、可见性和部位统计覆盖，而不是“有一个受影响顶点就算覆盖一个Patch”。不强求每种全局压缩局部化，也不强求每栋每个Patch受影响次数完全相等。
- 保留源面/顶点对应、UV像素干预掩码、操作顺序、实际几何位移及映射可信度。纹理掩码应回映射到所有使用相应UV像素的面，避免忽略UV复用传播。
- 大三角面支持分数面积/像素区域，不能用整面布尔选择让最低档预算突然覆盖整片立面。
- 几何删除区域在原模型侧可定位；待测网格不存在的面不能伪装成可预测Patch。实际推理定位可见缺口边缘或邻域，并分别报告完整性缺陷的定位边界。
- QEM的最近源面对应仅为近似，须记录误差/置信度，不能作为精确局部真值。

局部标签分三层：`intervention_support`（干预支撑）、`visible_defect_label`（可见异常弱标签/复核）、`patch_quality_label`（可选质量分）。**无可用Patch质量分时掩蔽对应回归损失，不把建筑总分复制给16个Patch，也不把面积比例直接叫局部质量真值。**

模型保留Patch Geometry/Texture/Overall Quality三个输出接口和6属性概率；只有标签支撑充分的输出才训练、报告为有效能力。现阶段优先验证局部异常定位，不能因接口存在就宣称局部质量预测已经完成。用户不需要逐Patch人工标注全部数据；后续少量边界样本复核用于检验弱标签可靠性。

## G. 标签、评分不确定性与类别均衡

每条记录最少包含：

```text
asset_id / entity_group_id / split / tile / content_digest
primary_class / combination_id / recipe_id / operations / seed
target_scale / machine_scale / human_scale / effective_scale / label_source
score_protocol_version / score_evidence / uncertain / technical_failure
applied_attributes[6] / visible_attributes[6] / attribute_valid_mask[6]
patch_layout_version / intervention_support / mapping_confidence
```

- 质量真值优先级：当前内容对应的人工有效分 > 独立AI建议；两者都缺失或不确定未解决时不进入正式有监督评测。
- `effective_scale`映射为 `(scale-1)/4` 只是序数标签的数值编码，不说明相邻档在心理感知上严格等距。模型输出可连续，但数据集本身仍只有5档标签。
- 已施加操作是provenance，不等于必然可见异常；模糊不清的属性标unknown并屏蔽损失，不把未知当正常。
- 模型采用6个Sigmoid而非Softmax，不以“干预脚本名字”强制生成one-hot标签；异常阈值仅在Val确定。
- 满额时每栋scale1～4各8个、scale5只有1个；全体对应1200/1200/1200/1200/150。训练用建筑均衡与等级均衡采样，必要时调整损失权重；不能复制Clean文件冒充更多独立样本。
- 保留 `single_dominant` 与 `combined` 标记，单类与组合分别评价。T3的缺纹理/色彩子组均衡轮换，按子组单列统计。

## H. 训练与评测协议

真实推理只输入待评 `.gltf + .bin + textures`。Clean配对、生成参数、源面对应只允许离线构造训练标签与诊断，不能进入模型推理特征。

1. 正式训练、全量特征提取在RTX4090服务器执行，不在Mac运行。复用合法现有缓存；资产内容或特征协议改变时重建相应缓存，不用旧缓存冒充新数据。
2. 固定seed2026，预先比较轻量基线、GATv2 Mesh＋较强纹理编码器、Face→Patch→Building层次模型＋几何纹理交互；分阶段验证，不一次铺开大量组合。只由Val选择最终候选。
3. 损失包含质量回归、5档序数辅助、有效等级排序、6属性多标签及有效Patch弱监督。机器建议与人工标签分别统计，不能隐去来源差异。
4. 先做在线glTF与缓存推理等价审计、模型与Val校准冻结，再一次性评测Test/Blind；不因结果不好继续调整同一测试版模型或标签。
5. 质量报告SRCC、PLCC、MAE/RMSE、QWK和等级混淆。只有5档且大量并列分，必须报告这一限制；单一质量档内真值恒定时不计算SRCC/PLCC，而报告误差和样本数。
6. 同时报告单类、组合、6类/子机制、各等级、建筑/图幅与尺寸分层结果；置信区间按独立建筑聚类bootstrap，不把同栋33个相关样本当33栋独立对象。
7. 异常报告每类及macro的Precision/Recall/F1、AUPRC；Patch只在标签有效区报告定位指标，弱掩码成绩不能替代主观局部质量验证。
8. 当前以机器辅助标签快速迭代，可检验对该标签体系的拟合；不能据此声称与人类感知高度一致或已建成MOS数据库。最终投稿若主张感知质量，仍需独立人工验证方案，人数与样本量另定；本轮不安排用户全量人工标注。

## I. 存储、内存与版本管理

所有资产、清单、评分、预览、Patch标签、生成临时文件、数据缓存及实验结果均放在移动硬盘：

```text
/Volumes/SANDISK-ELE/UrbanPhotoMeshQA-Data/HK3D-Individualised-V3/
  assets/<asset_id>/clean/
  assets/<asset_id>/<新的6类33版本修订号>/assets/<variant_id>/
  manifests/<dataset_version>/
  ratings/<dataset_version>/
  patch_targets/<dataset_version>/
  audits/<dataset_version>/
  previews/<dataset_version>/
  _review/<calibration_version>/
  _scratch/
  experiments/final/ 与 experiments/ablations/
```

- 项目代码与小型方案文档可留Mac，数据不复制到本地项目或系统临时目录。启动前核验移动硬盘真实挂载；未插盘则提醒并停止，不在`/Volumes`下误建替代目录。
- 原始资产独立可打开，包内glTF使用相对BIN/纹理路径；持久清单优先数据根相对路径，以适配GPU挂载。
- 标签绑定内容哈希；新生成样本不继承旧人工分。原文件名scale前缀不再作为唯一真值来源，手工改名只在明确导入、校验后成为一条有来源的评分记录。
- 用户已要求不再渲染示例页面：后续不自动打开浏览器或并行加载模型网格。必要视觉质检使用少量离线视图/局部证据，写到外置盘，顺序处理并限制内存；这不授权持续自动渲染网页。
- 历史process_v2/v3及早期失败目录不混入新Manifest；不自动删除已有数据。本轮仅更新文档，未做迁移或清理。
- 代码走Git，glTF/纹理/缓存/Checkpoint通过外置盘或GPU的受控文件同步；不将数GiB二进制反复提交普通Git历史。

## J. 执行顺序与验收停止点

| 阶段 | 工作 | 通过标准 |
|---|---|---|
| 0 本次 | 更新统一方案 | 用户可核对6类、33版本、150栋及标签边界；不执行生成 |
| 1 校准 | 准入3栋开发建筑，适配6类配方与8组合 | 每栋原模型合格；四档独立评分对应；组合有效；失败数公开 |
| 2 小批量 | 仅在预分配Train内选择12栋试生产 | 检查单位、UV、多材质、四档满额率、空间覆盖、磁盘与时间成本 |
| 3 冻结 | 冻结评分标准、配方搜索范围、版本与Split | 不再用Test/Blind或待评模型表现反向修改规范 |
| 4 正式生成 | 顺序完成120＋30栋，去重与分级审计 | 每栋33个合格版本，最终4950条；不合格不能凑数 |
| 5 GPU实验 | 小量预注册模型比较、Val选模 | 诊断有效标签与模型欠拟合/过拟合，不开展无逻辑大搜索 |
| 6 最终评测 | 推理等价检查、模型冻结、解锁评测 | 一次正式Test/Blind；保留机器/人工标签来源与局部监督限制 |

生成后做全量轻量文件依赖、解析和指纹检查；昂贵视觉检查聚焦开发校准、预设分层抽样及异常样本。不再重复无版本变化的全量昂贵审计。新配方尚未完成，不能把现有50版本直接重命名为33版本交付。

## K. 已实现与待实现的边界

已知现状：新候选A的`process_v3`已生成50版本、50内容唯一，具有机器建议分；其余历史版本仍按旧机制组织。`v4`配置及部分分数面积/起点改动属于先前未完成的开发，不是已验收的6类数据集。

本次只更新本文。后续还需实际实现6类调度、33版本清单、组合配额、独立分档验收与失败处理、异常标签新语义及数据迁移适配。模型接口也必须显式升级标签schema：旧“Texture Registration含色差”不能直接当成新T2，旧“Texture Missing”不能直接当成完整新T3。

---

## L. 全流程代码review与优先级待办（2026-09-06补充，仅方案）

本节基于本地源码静态检查，以及此前3栋现有Patch缓存的轻量统计；未运行GPU训练、未检查远端工作区、未执行全量数值回归、未渲染网页。以下“已确认”指源码中的行为存在，不代表已经证明它降低模型指标，也不代表历史发布模型均受影响。具体修复仍待执行与验证。

### L.1 拟采用的Patch划分与退化选区方案

当前 `src/urbanphotomeshqa/patches.py::topological_patch_layout` 已具备完整、不重叠覆盖、表面邻接和面积平衡生长，应该保留这些优点。问题是通过空间MST连接原本断开的部件后，只验证增强图上的连通，不能称为所有Patch都真实边连通；面积均衡也不是严格保证。

此前3栋 `process_v2/source_patch_layout.npz` 的轻量统计：

| 建筑 | 真实连通部件 | 虚拟桥 | 真实不连通的Patch数 / 16 | 单Patch最多部件 | Patch面积占比范围 |
|---|---:|---:|---:|---:|---|
| 原建筑 B355201752601063A0 | 5 | 4 | 3 | 3 | 1.22%～8.39% |
| 候选A B415722108801063A0 | 17 | 16 | 2 | 15 | 3.45%～10.25% |
| 候选B B421142104901063A0 | 23 | 22 | 4 | 20 | 2.41%～11.78% |

不连通不必然不合理：窗框、栏杆等可与附近立面组成一个语义局部区域。但不能只因固定Patch预算，就无约束地跨空隙、跨朝向连接较远部件。候选B每Patch面数6～700也说明仅提高Patch数量未必增加有效信息。

推荐实现 **结构约束的表面分区，固定16个Patch**：

1. 以真实Face邻接为主，采用表面距离与面积预算；明显法向转折作为跨界软惩罚，使楼角、屋顶与立面更易分开，而非直接按材质/UV岛切块。
2. 虚拟连接单独标记，只允许符合距离、空间跨度与朝向约束的邻近部件归组；孤立碎片保留可识别身份。跨面数/尺度自适应的阈值在开发集确定，不固定按世界单位套所有建筑。
3. 每栋固定划分16个Patch，不另建粗细Patch层。低面数区域不为追求面积绝对相等而强行切碎，正常面、零面积面、孤立碎片各有有效性说明。若资产有效面不足以形成16个有效Patch，则标记不适用于正式数据准入；推理接口可用无效槽位mask保持16槽输出，不能把空槽冒充有效Patch。
4. 几何邻接决定主体分区；UV/纹理区域通过面映射附着。共享UV像素影响多个面时全部追踪，不让材质打包决定模型空间分区。
5. 用Patch辅助选择多个攻击区域、平衡覆盖，但退化掩码在面/顶点/纹理像素层生成，可跨Patch边界。避免缺陷轮廓与Patch轮廓总是重合而形成捷径。
6. 推理Patch仅从当前资产生成。Clean对应只用于离线标签投影，重新编号、面排序或布局版本变化后必须重新映射。

划分验收除全覆盖/无重叠外，增加真实与虚拟连通分别统计、面积CV、空间直径、跨锐边比例、有效面数、重排面序及轻微坐标扰动稳定性。只比较现有16个Patch与改进后的16个Patch，依据Val的定位、质量和成本选择划分规则，不进行Patch数量消融。

### L.2 P0：标签与协议正确性，正式生成/训练前解决

| ID | 已确认行为及代码证据 | 待优化内容 | 验收要求 |
|---|---|---|---|
| P0-01 | `scripts/build_v3_process_pilot.py`仍按旧机制生成50版本；v4配置仍是旧细类字典，没有33版本调度及目标档独立验收循环 | 实现6类、8组合、目标/实际评分分离、多区域预算与有上限搜索；失败显式返回 | 3栋校准逐槽位检查，配方、内容和评分来源可追溯；未达标不报完成 |
| P0-02 | `scripts/compose_local_patch_quality_targets.py`依据旧GEOMETRY/TEXTURE集合，把非对应攻击的分项填为`ones(16)`，Overall取min | 新组合/副作用不能按名称默认另一模态满分；使用实测有效标签和逐模态valid mask | 混合异常、未知标签、原模型先天缺陷测试；缺标签不变成满分 |
| P0-03 | `scripts/train_real_gltf_quality.py`为互斥attack分类，`scripts/infer_real_gltf_quality.py`仍用softmax；旧类语义与新T2/T3不一致 | 版本化6属性、多标签Sigmoid、unknown mask、Val阈值；将旧强度辅助头与感知质量等级区分 | 单异常/多异常/无明显异常均可表达；旧checkpoint不得静默加载成新schema |
| P0-04 | `scripts/calibrate_quality_scores.py`默认通过Store加载所有Split，并预测、输出Val/Test/Blind，虽只用Val拟合却写“remain locked” | 校准仅加载Val及必要Train统计；最终评测独立入口，显式授权与冻结清单 | 未授权时Test/Blind标签文件即使不可读，校准仍可完成；不输出锁定集结果 |
| P0-05 | `scripts/extract_local_patch_features.py`对纹理攻击按attack标签从`clean_layouts`取布局；实时路径由当前Mesh重新划分 | 缓存和推理统一调用单模型布局；缓存复用需证明几何、面序、布局签名一致，不能依赖attack身份 | 组合、面序重排、纯纹理改动和不带Clean的独立输入等价；现行为不直接断言参考泄漏，先实测 |
| P0-06 | `train_local_patch_quality.py::loss_value`对纹理mask选出的张量直接取回归均值；`statistics`对空有效集合直接mean/std | 全空纹理监督/缺贴图/少面样本显式处理，所有模态分别屏蔽；空集合不产生NaN或虚假标签 | 空mask、全无纹理、部分无UV、批次无某类标签的单测，有限loss/梯度 |
| P0-07 | `extraction_signature`仅散列设置；旧缓存路径与标签连接多用`asset_id/attack/level`；`FORMAL_COUNTS`固定3493旧协议 | 样本主键加入dataset/schema/variant/content版本；特征签名加入代码、依赖、模型权重、布局和渲染协议；正式计数来自冻结Manifest | 新资产/代码/布局变化不能误复用旧缓存；同建筑多配方不撞路径；不覆盖历史3493验证入口 |

P0-04说明的是入口防护风险，不构成“历史实验已经按Test调参”的结论；修复时须区分旧发布脚本与新实验默认入口，不改写历史结果。

### L.3 P1：定位可信度、单模型一致性和生成真实性

| ID | 证据/风险 | 待优化与验收 |
|---|---|---|
| P1-01 | `patches.py`并存空间KNN重叠Patch与拓扑全覆盖Patch；旧raw提取走前者，local-v2走后者 | 标注、特征、推理与报告显式注明layout schema；按L.1统一新接口，不把两类Patch索引混用 |
| P1-02 | `local_features.py`、`train_local_patch_quality.py`、局部target脚本与推理分散定义16、固定数组及面积缩放 | 将固定16协议、布局版本、mask和归一化统一定义并写入checkpoint；校验所有入口一致，测试不足16个有效区域时的无效槽位处理；目标是统一协议而非增加数量 |
| P1-03 | `review_patch_data.py`用面中心最近邻映射；`build_patch_geometry_targets.py`用最近当前面承接Clean点；两者不是同一种精确对应 | 优先保留真实面来源/折叠历史，再使用带阈值的三角面投影或近邻；单列缺失区、跨墙误配和低置信映射，不将网页显示当精准训练真值 |
| P1-04 | v4 `surface_mask`支持分数面，但生成支撑仍主要写布尔`geo_affected/tex_affected`；区域填色与受影响面积口径可能不同 | 同步保存实际像素mask、分数面覆盖、操作后有效范围及UV复用回投影；测试部分大三角面、Repeat UV、多材质和跨Patch传播 |
| P1-05 | QEM按材质分开简化，放开边界后可出现裂缝；缺面后的整体拓扑/面序会变化 | 记录实际减面比、裂缝/零面积与副作用；稳定的材质边界处理和对应传播；不能只以“4个哈希不同”验收四档真实性 |
| P1-06 | 多种纹理机制涉及颜色空间、采样器、透明度与原生尺寸；当前JPEG主动拒绝透明贴图是已有防护 | 继续保留拒绝/降级机制，统一sRGB与线性处理、Repeat/Clamp、alpha和无贴图材料；不要因流程跑完就默认跨资产视觉等价 |
| P1-07 | `infer_real_gltf_quality.py`写死原始提取设置，并直接使用Base的identity输出；训练Store支持不同`base_representation` | 推理从checkpoint读取并校验完整特征协议，未知representation拒绝运行；对每个正式候选做raw/cache/final-output等价审计 |
| P1-08 | 评分服务已有digest绑定和人工优先，但最终训练入口仍主要读取旧NPZ目标而不是新评分schema | 建立统一标签导出器，校验未知/撤销/内容变更/重复样本/目标与实际档分离；不让旧quality占位值意外进入训练 |

### L.4 P2：在正确性通过后进行的模型与评测升级

以下三项纹理特征修正属于 **P1，优先于更换更强纹理骨干**，统一编号继续列入待办：

| ID | 本轮代码review发现 | 优化与验收 |
|---|---|---|
| P1-09 | `local_features.py`先用`textured`限制Patch视图mask，再传入`texture_quality_statistics`计算前景与纹理覆盖率；非空区域覆盖率因此退化为1，丢失无纹理表面的覆盖信息 | 分别建立可见表面mask、有纹理mask、缺失/无效纹理状态；合法纯色材质不自动判异常。测试局部缺纹理、全无纹理、合法纯色、遮挡和空视图，确认覆盖统计及valid mask正确 |
| P1-10 | `texture_features.py::patch_texture_atlases`将全模型所有可用材质等分进固定224×224图集；289材质时单格约13×13像素，且多数材质不属于当前Patch | 按每Patch实际使用的UV区域裁剪、分配采样预算和必要的分块编码，保留纹理尺度信息；用多材质数量分层样本验证细节保真、内存和耗时，避免编码器先制造模糊再预测模糊 |
| P1-11 | 同一图集函数逐UV顶点调用wrap后画多边形，跨Repeat边界的三角形存在错误连接风险 | 与生成端统一平铺/裁剪和坐标约定，覆盖Repeat、Clamp、镜像与跨边界案例；未支持的模式显式拒绝，不静默产生错误掩码。用可控UV样例验证像素覆盖和Face回映射 |

以上属于源码可确认的处理方式及其风险，尚未通过GPU实验量化对准确率的影响。不得把“已发现问题”写成“已修复并提升指标”。

- **质量专用特征，而非按名字删除Base。** `model.py`存在identity头，正式旧推理会使用它；这不证明它无用。先比较质量敏感表示与identity表示的Val表现，再做受控部分解冻。保留已证实有用的几何结构特征，不把“专注质量”理解为盲目重写全部骨干。
- **层次聚合与几何纹理交互。** `LocalPatchHead`已有可选cross-attention，不能把现有能力再当全新实现。补齐可信局部标签、空间邻接、Face→16个Patch→Building聚合与缺失模态mask后，比较是否实际改善定位和OQI；这里的层次指面、Patch、建筑，不是多种Patch数量。
- **关注难例而非只加参数。** 建筑均衡、等级均衡、原模型稀少、组合样本有效性、跨机制同档一致性优先；对固定16下的不同划分规则及不同Base做少量预注册消融。无标签支撑的Patch质量头不纳入有效能力宣称。
- **相关系数边界。** `train_real_gltf_quality.py::correlation`在常量/不足样本时返回0；新报告应返回null并给出原因，避免把不可定义当差表现。并列秩已有平均rank实现可保留；补5档QWK、多标签指标与按建筑bootstrap。
- **评测分层。** 旧局部评价按固定GEOMETRY/TEXTURE集合选mask，升级为实际属性及valid mask；区分单类/组合、异常类型、局部可见程度和置信映射。不要仅凭生成类别推定每个分项的有效监督。
- **资源与可靠性。** `extract_local_patch_features.py`先积累全部rows再输出，新规模下应分片流式写外置盘/GPU磁盘；失败可恢复但恢复键须包含协议版本。生成器在读写前核验挂载，临时文件/模型下载缓存显式定向外置盘；不启动网页压力测试。所有大型运行仍只在GPU服务器执行。
- **代码收敛与发布。** 现推理从训练脚本导入模型类，后续将正式模型/数据schema/预处理移到可复用包，CLI做薄封装；发布绑定特征与标签schema、依赖、权重hash、Val校准和异常阈值，防止脚本版本漂移。

### L.5 实施顺序与完成定义

1. 先做新Manifest/标签schema和锁定协议（P0），同时补空mask、重复主键和cache签名单测。
2. 再做固定16个Patch的结构约束布局、精细掩码/对应和多区域退化（P1）；用3栋小规模验证，不渲染网格网页。
3. 六类33版本与四档验收通过后，才按J节进入Train小批量与正式生成。
4. 修正P1-09～11纹理特征后，补齐整栋/逐Patch多标签头，在GPU比较现有基线、改进Patch、质量敏感Base与交互模块。
5. 在Val选择并校准异常阈值，进行分阶段推理性能测试和受控加速；最后冻结候选、做推理等价审计、解锁一次评测。具体输出和速度验收见M节。

本节所有待办均未因写入文档而视为已完成。后续逐项记录修改commit/代码版本、对应测试、数据影响和验收结果；不重复已完成实验，不把本次静态review冒充完整系统测试。

## M. 最终能力目标：质量、快速Patch定位及两级异常类型

### M.1 现状与待实现输出

`scripts/train_local_patch_quality.py::LocalPatchHead`当前只返回Geometry、Texture、Overall三个局部分数，没有逐Patch异常类型头；全局旧`QualityHead`及推理路径仍为互斥异常分类。现有质量分或网页生成支撑热力图，不等于已经实现下面的异常检测能力。

全流程固定16个Patch，拟统一输出：

| 层级 | 输出 | 限制 |
|---|---|---|
| 整栋 | Overall OQI、有效的几何/纹理质量、6类异常概率及命中类别 | 分项质量须有有效监督；异常允许同时出现多类 |
| 每个Patch | Geometry/Texture/Overall质量接口、6类异常概率、命中类别、可评估状态 | 固定16槽；未知、无效或证据不足不得冒充正常 |
| 空间解释 | Patch布局版本、面成员关系或索引文件、中心、面积比例、异常热力图所需数据 | 可同时标红多个Patch；Patch分数不能声称具备像素级预测精度 |

两级异常头采用各自的6维Sigmoid，多标签损失带有效性mask；不设Mixed、不对16个Patch做互斥softmax，不强制只找一个最差Patch。建筑级与Patch级可增加受控的一致性约束，但不能把建筑总标签复制给所有Patch。局部质量、局部异常分类是不同任务，标签缺失时分别屏蔽损失。

### M.2 置信度、阈值与标红规则

1. 对有效Patch的第c类输出`p_patch[c]`。若超过已冻结的该类Patch阈值`tau_patch[c]`，报告该类异常；若任意一类命中，该Patch标为“检测到退化”。多个Patch可同时命中，多种类型可叠加。
2. 建筑级单独使用`p_building[c]`与`tau_building[c]`，不能默认两级阈值相同。阈值只在Val选择，优先按预设Precision/Recall代价或F1目标确定，不以Test/Blind试阈值。
3. 质量分不等于异常概率，禁止用`1-quality`冒充退化置信度；注意力权重也不等于异常概率。若要独立输出“任意异常概率”，应另行校准相应输出，不把简单max/sum当作已校准联合概率。
4. Sigmoid数值先称模型概率分数。使用可靠性图、Brier score/ECE等检查；需要时用Val内预设子划分/交叉拟合做概率校准与阈值选择，避免小样本下反复挑选。0.9未经验证不能表述为90%判断正确。
5. 低于阈值仅表示“未检测到”，不保证完全无缺陷。对不可见、纹理不可用、布局或输入异常的Patch输出可评估状态，不能以低置信度自动证明正常。
6. Checkpoint/发布包保存类别顺序、标签schema、两级阈值、校准方法及参数、适用数据版本。推理参数不允许静默覆盖正式阈值；原始概率、校准概率和最终判定可追溯。

### M.3 快速推理优化（需测量，不预先承诺实时）

源码可见的重复工作包括：`infer_real_gltf_quality.py`每次调用加载多个checkpoint/编码器，全局提取后为局部再次读取Mesh；全局与局部分别准备图像特征。渲染函数也有逐面CPU处理和纹理解码路径。上述是潜在成本来源，**尚无本轮GPU端到端时延测量，不能声称哪个阶段已被证明是最大瓶颈**。

拟按以下顺序优化：

- 建立常驻推理服务或可复用预测器，一次加载权重和预处理配置，支持连续资产；保留单文件CLI作为薄封装。
- 单资产只解析一次、统一16-Patch布局，复用解码纹理和相同协议的图像特征。全局/局部渲染分辨率或视角不同，须先统一或明确验证，不能强行共享不等价特征。
- 保留 `SpatialImageEncoder.patch_tokens` 已有的“同视图共享特征图再按Patch池化”；16个Patch批量预测，而非逐Patch重复运行编码器。
- 对Atlas裁剪与图像编码分块、批量处理，按有效区域分配预算，限制显存与内存；全局和局部头共享有用特征，不盲目增加双份骨干。
- 先做阶段计时，再决定GPU光栅化、混合精度、编译或推理格式导出是否值得。后端变更必须测试贴图/UV/alpha一致性及预测、阈值附近判定误差；不以速度优化名义跳过质检。
- 逐项测量文件读取/解析、纹理解码、Patch划分、必要渲染、特征编码、预测头、后处理及总耗时；报告冷启动和常驻热启动、batch=1延迟p50/p95、峰值内存/显存，以及建筑规模和硬件条件。
- 区分真实单glTF端到端速度、只读特征缓存速度和仅网络forward速度。缓存只能使用当前输入自己的内容哈希，不读取Clean、退化标签或预设分数。

本轮没有约定“多少毫秒/多少FPS”的指标，后续在RTX4090上建立基线后再制定预算；所有性能测试遵守Mac不做大规模实验、数据和临时输出放外置盘/受控GPU存储、不自动渲染示例网页的要求。

### M.4 论文验证与验收证据

核心目标不是“画出红色热力图”，而是同时证明质量评价、异常类型与定位的有效性及实际计算成本：

- 分开验证建筑级与Patch级6属性，报告每类/macro AUPRC、Precision/Recall/F1、阈值及有效样本数；不让多数正常Patch掩盖少数异常。
- 同时报告单类、双/三类组合、新建筑、跨图幅表现；组合操作日志须与最终可见异常区分。对局部标签来源、映射置信度和不可评估比例分别统计。
- 生成掩码上的高分只能证明与弱标签一致。若论文主张真实可见缺陷定位准确，应有独立视觉复核证据；机器分上的高SRCC不能代替人类感知验证。
- 做少量预注册消融：可信纹理特征修正、改进16-Patch布局、全局/局部共享、两级多任务、几何纹理交互、概率校准。比较准确率与端到端时延，不以参数量或增加模块本身作为创新证据。
- 正式可视化只标注模型真正输出的能力；最终Test/Blind结果和性能报告在模型、协议、阈值冻结后生成，不能反向修改同一版本。

本节为方案待办，与L节优先级联合执行；当前未增加分类头、校准器或常驻服务，未开展新训练/性能实验。

<details>
<summary>历史讨论与执行记录（已被上述当前方案取代，保留追溯，不作为执行协议）</summary>

## 历史：纯人工标注城市Mesh质量数据集

> 状态：方案草案，仅编写，不执行  
> 日期：2026-09-05  
> 随机种子：2026  
> 核心原则：本轮只使用人工标注质量分数训练，不使用机器Pseudo-MOS作为训练真值

V3持续修订的评分标准见：`docs/V3_人工质量评分规范_ZH.md`。当3栋校准建筑完成交互评分后冻结该规范，后续不得依据Test或Blind结果改变标准。

> **协议更新（2026-09-06）：** 本文下方原有的`60栋、600条、每栋9个退化`内容属于早期讨论稿，已停止采用。当前候选正式协议为每栋`10类单一退化×4档＋9种组合退化×1档＋1个Clean＝50个版本`；主集120栋共6000条，另设30栋跨图幅Blind共1500条。数据规模、退化集合、评分流程和冻结条件均以`docs/V3_人工质量评分规范_ZH.md`的最新版本为唯一依据；在3栋校准建筑完成前仍可修订，冻结后不再随实验结果改变。

## 1. 本轮目标

迭代3建立一个规模较小、标签可信、完全针对城市摄影测量建筑Mesh的人工质量数据集，并以它作为模型训练、验证和最终测试的唯一质量监督来源。

本轮不引入飞机、动物、人物、器皿等通用物体数据集。研究对象保持为：

> 香港城市摄影测量Individualised Building glTF Mesh的无参考质量评价。

模型实际推理仍然只允许输入一个待评估资产包：

```text
.gltf + .bin + textures
```

推理阶段不得输入Clean参考模型、攻击类型、攻击强度、Clean—Attacked差值或配准结果。

## 2. 历史数据规模协议（已废止，仅保留决策记录）

本轮正式数据集固定为`600`条：

```text
60个高质量Clean建筑
每个建筑生成9个退化版本
= 60个Clean + 540个退化样本
= 600条正式样本
```

为避免歧义，本方案将“每个数据集做10种退化”解释为：

> 每栋建筑最终有10个质量版本，即1个Clean版本＋9个退化版本。

如果实际意图是“每个Clean另外生成10个退化版本”，总数将变为`60＋600＝660`条，与“剩余540条”不一致，因此本轮暂以600条协议为准。

本轮不缩减为30栋建筑。无参考质量评价需要跨建筑学习质量规律，建筑数量过少时容易记忆建筑形态、图幅和纹理风格。现阶段固定采用60栋建筑，以建筑多样性优先于单栋攻击数量。

这600条同时包含Train、Val和Test。本轮首轮不从现有3312条机器生成样本补充训练数据，也不混入迭代2的Pseudo-MOS；它们只保留作历史对照。

## 3. Clean建筑筛选

### 最新补充：独立质量准入与两栋试运行门槛（2026-09-06，仅记录方案，暂不执行）

本补充优先于本文历史筛选、默认评分与批量执行描述。当前只更新文档，不启动候选筛选、退化生成、评分修改或训练。

**先独立筛Clean，再评退化版本。** “原始模型”只表示未施加本项目的退化，不表示本身必然高质量，更不能自动赋予scale5。

1. **Clean独立准入。** 在一致的渲染设置和可比观察尺度下，检查多方位整体视图、各主要立面、屋顶及纹理近景。逐项记录纹理清晰度、拉伸、重影、色差/接缝、缺失及几何完整性；不得只依赖低分辨率缩略图、一个最好看的角度、面数或文件完整性。只有多视角质量稳定、无明显先天缺陷并经用户确认合格后，才作为scale5参照。允许不影响主要观感的少量局部瑕疵，不以像素级无缺陷为标准。
2. **退化版本按实际观感评分。** 参考用户既有人工样例，同时考虑缺陷严重程度、受影响范围和部位重要性。Clean对照仅用于辅助识别变化，不得把“与Clean差异小”直接等同于绝对质量高。当前基于六视图RGB差异、Clean默认5档的机器建议只能视为未校准启发式，不能作为Clean准入证据，也不是人工MOS。
3. **参考既有二维CIM标注，但不照搬。** 参考目录为`/Users/wangfugui/MaosuLi/3D-Demo/results/quality/human_labeled_500`，以最终人工标签为准；历史评论与最终档位不一致时不得覆盖最终标签。借鉴其感知严重程度与分档样例，不直接迁移构图、视野遮挡或景观观感等二维因素；三维单体需独立检查多个表面。借鉴评分规则不等于将这批二维图片加入Mesh训练集。

**现有三栋的处理计划：**

- 新增的`B360381775901063A0`和`B354401777301063A0`暂不作为正式高质量锚点，后续按上述标准重新筛选替代候选。
- 原第一栋`B355201752601063A0`也须按同一标准复核，不沿用默认scale5作为已合格证明。
- 保留现有资产与标注以便追溯；本次文档更新不删除文件、不修改网页分数或数据状态。

**后续执行顺序与停止点：**

1. 获准开始后，先挑选两栋高质量候选，提供整体多视角、立面/屋顶近景、缺陷检查记录及建议结论，作为独立Clean准入流程的试运行。
2. 由用户核实这两栋及筛选标准。若不通过，仅调整这两栋候选或检查方法，不扩大筛选规模；未经确认不为新候选批量生成退化。
3. 只有用户明确核实通过后，才按确认的标准筛选剩余Clean；退化版本随后沿用每栋50个版本的候选协议，并按实际观感评档。

两栋试运行是批量筛选的前置验收门槛，不代替原有评分规范冻结、Val选模与Test/Blind锁定要求。

**后续授权执行记录（2026-09-06）：** 用户随后明确授权选择两栋、生成样本并评分，以及重评原建筑。本次完成两栋各50个审阅位置与原建筑50条AI视觉重评；不是批量筛选授权。候选A/B的Clean建议5档、原建筑建议4档，均待用户核实。两栋新候选因QEM停滞共97个独立内容，重复已标记；平滑真实性等问题尚未冻结。详见`docs/V3_三栋视觉复核_20260906.md`。此前“仅记录、不执行”描述保留为当时的决策记录，当前仍禁止未经用户验收扩展到剩余Clean。

### 3.0 V3数据根目录

迭代3的全部源数据、退化资产、清单、审计和标注辅助文件统一放在：

```text
/Volumes/SANDISK-ELE/UrbanPhotoMeshQA-Data/HK3D-Individualised-V3/
├── assets/
│   └── <asset_id>/
│       ├── clean/
│       │   ├── clean_scale5.gltf
│       │   ├── buffer_*.bin
│       │   ├── textures/
│       │   └── metadata.json
│       └── degradations/
│           └── scale1/ ... scale5/
│               └── <degradation_name>/
│                   ├── <degradation_name>_scaleN.gltf
│                   ├── buffer_*.bin
│                   ├── textures/
│                   └── metadata.json
│       └── rating_workspace/
│           ├── scale5_clean.gltf
│           ├── scaleN_<degradation_name>.gltf
│           ├── _resources/
│           └── rating_workspace_index.json
├── manifests/
├── audits/
├── previews/
├── human_ratings/
├── patch_targets/
└── _review/
```

每个资产包必须独立可打开；不得依赖V3目录之外的BIN或纹理文件。`_review/`只保存候选筛选材料，不进入正式Manifest。

人工评分目录统一使用`scale1`～`scale5`，不使用“很差/较差”等中文目录名。用户拖动某个退化资产目录改变档位后，正式标签冻结工具必须同步更新`.gltf`文件名、metadata和Manifest中的`quality_scale`，避免目录档位与文件名不一致。

为提高查看和改分效率，每栋建筑额外生成一个扁平化`rating_workspace/`。其中10个glTF位于同一层，文件名统一为`scaleN_<异常类型>.gltf`；全部BIN和纹理依赖保存在`_resources/<variant>/`下，并由glTF使用相对路径引用。用户只需修改文件名前缀，例如将`scale2_geometry_hole.gltf`改为`scale3_geometry_hole.gltf`。正式标签冻结时以文件名前缀为准，反向同步正式资产metadata和Manifest。`rating_workspace`是查看与标注副本，不替代`clean/`和`degradations/`中的正式独立资产包。

### 3.1 Clean必须是真正的高质量模型

60个Clean不能简单随机抽取。每一个Clean都必须经过严格筛选，确保其本身可合理标记为第5档“优秀”。

候选来源：

```text
/Volumes/SANDISK-ELE/HK3D-Individualised
```

或者已审计、保持原始质量的迭代2城市建筑源目录。筛选时不得从公开通用物体数据库补充飞机、人物、动物等非城市对象。

### 3.2 Clean入选条件

入选建筑应同时满足：

- glTF、BIN及全部纹理依赖完整，可独立打开；
- 主体建筑完整，没有大面积缺面、孔洞或漂浮结构；
- 墙面、屋顶和主要轮廓自然，没有明显尖刺、波纹或错误变形；
- Mesh密度与建筑结构匹配，没有肉眼可见的过度简化；
- UV和材质引用正常，没有明显纹理错位、拉伸或接缝错误；
- 纹理清晰度较高，没有大面积模糊、空白、黑块或重影；
- 多个标准视角下质量稳定，不依赖某一个“最好看的视角”；
- 不存在明显遮挡残留、地面粘连或与主体无关的大块噪声；
- 文件格式和解析过程没有技术异常；
- 人工复核后能够明确归入第5档，而不是勉强接近第5档。

任何原始模型只要存在明显先天缺陷，都应淘汰，不能因为它名义上是“原始数据”就自动当作Clean。

### 3.3 60个Clean的多样性

在保证高质量的前提下，60栋建筑还应覆盖：

- 多个香港图幅；
- 高层、低层、塔楼、裙楼、复杂屋顶等不同形态；
- 简单立面和复杂立面；
- 不同模型尺寸、三角面数量与纹理分辨率；
- 不同材质数量和UV覆盖复杂度；
- 不同拍摄条件与城区环境。

不能只选择外观相似、面数相近或来自同一图幅的60栋高质量建筑，否则模型容易学习建筑内容而不是真实质量。

### 3.4 Clean筛选记录

正式执行时，每个候选都需要保存：

```text
asset_id
tile/sheet
文件完整性审计
顶点数、面数、材质数、纹理分辨率
六视图预览
人工入选/淘汰结论
淘汰原因
```

在60个Clean名单正式冻结前，不生成其退化版本。

## 4. 人工质量标签

### 4.1 五档定义

本轮使用5档人工质量等级：

| 等级 | 名称 | 含义 |
|---:|---|---|
| 5 | 优秀 | 几乎没有可察觉缺陷，可直接交付使用 |
| 4 | 良好 | 存在轻微缺陷，但不明显影响整体使用 |
| 3 | 一般 | 缺陷清楚可见，但主体仍完整可用 |
| 2 | 较差 | 严重缺陷明显影响观察或使用 |
| 1 | 很差 | 大面积损坏或强烈错误，基本不可接受 |

60个通过严格筛选的Clean统一标记为第5档。其余540个退化样本由用户本人在匿名、随机、不显示攻击参数的条件下人工评分。原则上使用第1～4档；但如果某个轻度退化在实际观察中与优秀Clean没有可察觉差别，应允许标为第5档，不能仅凭“它是攻击样本”强制降级。

建议数值映射为：

```text
1 → 0.00
2 → 0.25
3 → 0.50
4 → 0.75
5 → 1.00
```

模型最终仍可输出`0～1`连续质量分数，同时训练一个5档序数分类辅助任务。但本轮连续训练真值只能从人工标签产生，不混入Pseudo-MOS v3。

### 4.2 标注时不得泄漏的信息

人工评分界面或文件夹名称不得显示：

- Clean或Attacked身份；
- 攻击类型；
- 攻击参数；
- 预设light/medium/heavy强度；
- Train/Val/Test/Blind划分；
- 机器Pseudo-MOS或模型预测结果。

评分人只根据当前待测Mesh的可见质量进行判断。攻击生成时使用的强度档位不能直接当作人工质量档位，因为不同建筑对同一攻击参数的视觉敏感程度不同。

### 4.3 文件夹拖动标注

继续采用最快的文件夹拖动方式：

```text
Human-Rating-Iteration3/
├── 00_待评分/
├── 01_很差/
├── 02_较差/
├── 03_一般/
├── 04_良好/
├── 05_优秀/
├── 98_不确定/
├── 99_技术异常/
└── private_mapping.csv
```

拖动的是匿名评分条目或符号链接，不移动原始glTF资产包。540个攻击样本随机打乱，同一建筑的多个版本不得连续出现。

## 5. 九个退化版本的设计原则

本轮每个Clean生成9个退化版本：8个单一退化版本和1个联合退化版本，并通过跨建筑轮换轻、中、重参数覆盖不同可见质量区间。联合退化不是一种独立异常类型，而是同一样本同时具有几何异常和纹理异常。

正式攻击参数将在方案确认后另行冻结。本轮首版按以下8种基础退化机制加1个联合样本组织：

1. `geometry_hole`：局部缺面；
2. `mesh_simplification_qem`：LOD或发布优化造成过度减面；
3. `geometry_noise_spike`：表面起伏和尖刺；
4. `geometry_over_smoothing`：棱角、屋檐和几何细节消失；
5. `texture_detail_loss`：模糊、降采样或压缩造成细节损失；
6. `texture_region_missing`：局部纹理空白或缺失；
7. `texture_misalignment`：错位和重影；
8. `texture_seam_radiometric`：曝光、色温和亮度不一致造成拼接缝；
9. `geometry_texture_combined_sample`：从上述几何和纹理机制中各选择一种同时施加，仅表示联合样本，不增加新的异常类别。

参考YN2023与SJTU-TMQA后，V3正式生成还应在不增加6个异常属性数量的前提下轮换以下子机制：

- `Texture Detail Loss`：高斯模糊、Lanczos降采样、JPEG压缩；
- `Geometry Detail Loss`：QEM简化、过度平滑、顶点坐标量化；
- `Texture Registration`：局部错位/重影、UV坐标量化、辐射接缝。

这些子机制保存在metadata中用于细分分析，但首轮模型仍预测6个异常属性，不把具体生成算法当作互斥类别。

每种基础退化预定义轻、中、重三组生成参数，但每栋每种只生成其中一个强度。对60栋建筑均衡轮换，使每种基础退化最终包含：

```text
20个light + 20个medium + 20个heavy = 60个样本
```

单栋建筑总体分配3个light、3个medium和3个heavy。攻击强度只用于生成和平衡抽样，不能直接充当人工质量标签。

本轮还规定：

- 同时包含几何退化和纹理退化；
- 包含单一退化和少量符合真实场景的组合退化；
- 尽量让人工等级1～4均有足够样本；
- 不能只生成容易识别的重度攻击；
- 不能通过目录名、编码格式、文件大小等无关信息泄漏攻击标签；
- 攻击必须直接作用于Mesh、UV或原始纹理文件，不攻击最终截图；
- 每个攻击结果必须是可独立打开的glTF资产包；
- 生成后必须重新解析并完成人工视觉审计；
- Clean配对只用于数据生成和审计，不进入无参考模型推理。

顶点坐标量化、UV量化、文件损坏和依赖缺失暂不列为首轮质量类别：前两者可在首轮误差分析后决定是否追加；后两者属于资产完整性检测，不应混入可正常打开模型的视觉质量评分。

## 6. 数据划分原则

必须先按`asset_id`划分60栋Clean，再把对应9个退化版本继承到同一Split。同一栋建筑的任何版本不得跨Split。

首轮固定划分建议如下：

| Split | Clean建筑数 | 每栋版本数 | 总样本数 | 用途 |
|---|---:|---:|---:|---|
| Train | 40 | 10 | 400 | 模型训练 |
| Val | 10 | 10 | 100 | 选模、早停、校准 |
| Test | 10 | 10 | 100 | 唯一一次最终测试 |
| 合计 | 60 | 10 | 600 | — |

采用`40/10/10`，使每类异常在Train约有40个跨建筑样本，同时保留独立Val与Test。由于总建筑数只有60栋，本轮暂不从这600条中再切分Blind，否则每个Split的独立建筑数过少。跨图幅Blind留到首轮确认有效后从600条之外追加，且不得根据Blind结果反向调参。

Train、Val和Test允许包含相同图幅，但必须保证`asset_id`完全隔离，并在建筑形态、面数、纹理分辨率和图幅上尽量平衡。必须在生成攻击和人工评分前冻结建筑级划分；同一建筑的Clean及9个退化版本永远位于同一Split。

## 7. 纯人工标签训练协议

本轮训练只允许读取：

- 当前待评估glTF自身的无参考特征；
- 人工1～5档质量标签；
- 由人工档位映射得到的连续分数；
- 由攻击生成过程确定的6属性异常标签；
- 由面、顶点、UV和纹理掩码生成的Patch空间标签；
- 必要的样本匿名ID和Split信息。

其中，整体质量分数及等级监督必须完全来自人工；自动标签只监督异常属性识别和Patch空间定位，不能参与OQI真值计算。

本轮质量监督禁止读取：

- Pseudo-MOS v1/v2/v3；
- Clean—Attacked客观差值分数；
- 由LPIPS、Chamfer、法向差或纹理差直接合成的OQI；
- Test标签参与选模；
- 攻击类型和预设强度作为质量回归输入。

异常识别采用6个相互独立的属性标签，而不是Clean、Geometry、Texture、Mixed四选一分类：

```text
Geometry Missing
Geometry Deformation
Geometry Detail Loss
Texture Detail Loss
Texture Missing
Texture Registration
```

6个属性与生成机制的对应关系为：

| 异常属性 | 覆盖的退化现象 |
|---|---|
| Geometry Missing | 缺面、孔洞 |
| Geometry Deformation | 噪声、尖刺、局部形变 |
| Geometry Detail Loss | QEM简化、过度平滑 |
| Texture Detail Loss | 模糊、降采样、压缩 |
| Texture Missing | 纹理区域缺失 |
| Texture Registration | 错位、重影、曝光或颜色接缝 |

模型对6个标签分别使用Sigmoid和二元交叉熵。标签之间不互斥，因此一个样本可以同时输出多个异常。几何和纹理大类概率不额外建立互斥分类头，而是分别由同组3个属性概率聚合得到。

`Clean`和`Mixed`均不作为异常类型训练：当6个属性概率都低于各自阈值时，报告“未检测到明显异常”；多个属性超过阈值时同时报告。每个属性的阈值不预先武断固定为0.5，而是在Val上分别选择并冻结，Test不得参与阈值调整。

8种基础攻击名称保留在metadata中，仅用于生成审计和细分误差分析，首轮不要求模型预测攻击细类，也不能作为模型输入。质量回归成绩必须来自同一个仅输入待测glTF的无参考模型。

建议主损失由以下部分组成：

```text
人工连续质量回归损失
+ 5档序数分类损失
+ 同批样本人工等级排序损失
+ 6属性多标签异常损失
+ Patch多标签定位损失
```

模型选择只看Val，Test锁定。所有比较继续固定随机种子2026，避免在只有60栋建筑时进行大规模超参数搜索。

首轮只比较少量、事先写定的设置：先冻结特征骨干训练质量头；如出现明显欠拟合，再进行一次受控的部分解冻。不得因为Test结果不理想继续调参。由于Train只有40栋建筑，本轮不直接从头训练参数量很大的Transformer骨干。

### 7.1 模型代码适配要求

迭代3执行时需要对现有模型代码做适配，但本方案阶段不实施：

- 质量回归头改为读取人工连续分数；
- 增加5档序数分类头；
- 将原来的互斥攻击分类Softmax头替换为6个独立Sigmoid异常属性头；
- 数据加载器允许同一样本同时具有几何和纹理异常标签；
- 推理端分别应用Val冻结的6个属性阈值；
- 推理报告可同时列出多个异常，不再输出单一`predicted_degradation`；
- 保留Face→Patch→Building层级接口，使建筑级与Patch级异常共享6属性定义；
- 保持单glTF无参考输入，攻击名称和强度不得进入网络特征。

### 7.2 Patch局部异常定位

局部异常定位是本项目的重要研究目标，本轮建议纳入，而不是留到完全独立的后续版本。但不要求用户人工逐Patch标注：人工很难稳定判断三角面、UV区域与空间Patch之间的对应关系，成本高且一致性差。

Patch真值优先从攻击生成过程自动记录并投影得到：

- 几何孔洞：记录被删除三角面及其相邻受影响区域；
- 几何形变：记录被位移的顶点和关联三角面；
- QEM简化与过度平滑：记录受影响面或通过Clean—Attacked离线对应生成影响权重；
- 纹理缺失、细节损失、错位和接缝：记录纹理像素掩码，经三角面UV映射回Face，再汇聚到Patch；
- 联合退化：同一个Patch可以同时具有多个异常属性标签。

每栋固定16个Patch，当前及后续迭代不增加数量。Patch划分必须只依据待测Mesh自身且具有确定性；仅优化划分规则，不进行数量消融，不能使用Test选择划分规则。

这些自动生成的空间掩码只用于离线训练局部定位，不是机器Pseudo-MOS，也不替代用户的建筑级人工质量评分。实际推理仍只输入待测glTF，不读取Clean或攻击掩码。

每个Patch输出：

```text
Patch Geometry Quality
Patch Texture Quality
Patch Overall Quality
6个Patch异常属性概率
```

超过Val所冻结阈值的Patch标记为异常；热力图按异常概率或局部质量着色，高置信异常Patch显示为红色。允许多个Patch、多个异常属性同时被检出，不使用“只能选一个最差Patch”的互斥逻辑。

为了兼容以后少量人工局部复核，Patch标签格式必须同时支持`machine_provenance_mask`和可选的`human_verified`字段。本轮用户只需完成整体1～5档评分；建议后续仅抽查少量热力图是否定位合理，而不是手工标完整600条Patch。

### 7.3 首轮评价指标

质量预测以`Overall SRCC`为首要指标，同时报告PLCC、MAE和RMSE；5档序数任务报告相邻档准确率与Quadratic Weighted Kappa；6属性异常检测分别报告Precision、Recall、F1、AUROC，并报告多标签Macro-F1。Patch定位报告Patch级AUPRC、AUROC、F1和热力图可视化审计。Val用于选择唯一候选并校准各异常阈值，Test只运行一次。

除总体结果外，还必须报告：

- 各人工质量档位的误差；
- 各退化类别的SRCC、PLCC和MAE；
- 几何、纹理以及联合退化样本的表现；
- Clean与退化样本的混淆情况；
- 按建筑、图幅、面数和纹理分辨率的误差分布。

由于每个Test异常类别只有约10条，单类别相关系数仅作诊断，不能作为主要结论。主要结论以100条Test总体结果为准。

## 8. 人工标签质量控制

即使由用户本人标注，也需要控制评分漂移：

- 先完成约100条Pilot并固定档位标准；
- 随机重复插入约30～50条样本，检查前后评分一致性；
- 相同重复样本原则上相差不超过1档；
- 每次连续评分不超过30～40分钟；
- `98_不确定`集中二次复核；
- 技术异常样本不得直接标为1档，应进入`99_技术异常`并重新生成或淘汰；
- 保存原始拖动目录录、匿名映射和评分时间；
- 模型预测结果不得用于修改人工标签。

单人标签严格来说是Opinion Score，不是多人平均意义上的MOS。本轮可以称为“人工主观质量标签”；若论文最终使用“MOS”表述，仍建议邀请多名评分人对至少测试集或代表性子集独立复评。

## 9. 本轮阶段门槛

在执行开始前，至少满足：

1. 60个Clean全部通过格式审计与人工第5档复核；
2. 60栋建筑不存在跨Split泄漏；
3. 540个退化资产均可独立打开；
4. 人工标签只有1～5档，不混入机器Pseudo-MOS；
5. 人工1～5档不能严重失衡，并单独报告每档样本数；
6. Test在模型和超参数冻结前保持不可见；
7. 单glTF在线推理与训练输入协议一致。

## 10. 拟执行顺序

本文件当前只记录方案，不执行以下步骤：

```text
审计城市建筑候选池
→ 人工筛选60个真正高质量Clean
→ 冻结Clean名单和建筑级Split
→ 确认9个退化版本的具体攻击协议
→ 生成540个真实文件级退化glTF
→ 解析与视觉审计
→ 建立匿名评分工作区
→ 用户人工完成1～5档评分
→ 审计等级分布与重复评分一致性
→ 冻结人工标签
→ 仅使用人工标签训练
→ Val选模
→ 唯一一次锁定Test评测
```

## 11. 待确认事项

在开始执行前，需要最终确认：

1. 总规模是否确定为`600`条，即每栋`1个Clean＋9个退化版本`；
2. 九个退化版本是否采用第5节建议的“8种单一退化＋1种组合退化”；
3. 是否接受`40/10/10`栋的Train/Val/Test初始划分；
4. 本轮是否不设600条内部Blind，而将跨图幅泛化改为Train内留一图幅验证；
5. 人工评分是否先只标Overall，Geometry和Texture分项标签以后再补；
6. 论文正式阶段是否安排其他评分人复评测试集或代表性子集。

在上述协议确认前，不进行数据筛选、攻击生成、人工评分工作区创建或训练。

## 最新补充：2026-09-06 强度校准与三维总览

本补充记录用户后续授权，优先于上文历史“先不执行”及纯人工600条草案。继续机器生成退化；本次先对新候选 A 的50个版本校准，使用独立 `process_v3`，不继承旧人工分。其余建筑和正式训练暂未扩展。

详见 [V3 强度校准与总览页](docs/V3_强度校准与总览页_20260906.md)。强度1→4尽量覆盖质量scale4→scale1，但不能直接映射标签，更不能为凑档制造不合理损伤。局部类型分散起点，同类型四档保持可比较的位置。已增加可交互四档网格页面，分开显示机器、人工和采用评分。

</details>
