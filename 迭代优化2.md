# UrbanPhotoMeshQA 迭代优化 2（执行中）

> 唯一目标：提高单个城市摄影测量 glTF Mesh 的无参考质量评估、退化诊断与局部定位能力。
>
> 方案已由用户确认，于2026-09-03开始按门槛逐项执行；Test/Blind在唯一候选冻结前保持锁定。

## 1. 当前基线与核心判断

正式全局模型 OQI SRCC：Train `0.886`、Val `0.521`、Test `0.557`、Blind `0.444`。Train 与 Val/Blind 差距很大，当前首要问题是跨建筑、跨图幅泛化，而不是训练集拟合不足。

局部 v2 Geometry/Texture/Overall SRCC：Val `0.381/0.602/0.422`，Test `0.433/0.623/0.446`，Blind `0.468/0.578/0.432`。局部纹理已有可用信号，局部几何与综合排序仍弱。

当前 OQI 是由 Clean—Attacked 客观差异生成的机器质量真值，不是人工 MOS。已有例子显示不同退化的感知尺度不均衡：纹理错位惩罚可能过轻，而孔洞、区域缺失可能过重。若真值本身不合理，单纯提高 SRCC 没有充分意义。

本轮目标不是无逻辑堆模型，而是依次解决：

1. 机器伪 MOS 的感知合理性；
2. 独立建筑与图幅数量不足；
3. 质量、退化类型和强度的联合学习；
4. 跨图幅泛化；
5. 局部质量与全局质量的一致性。

## 2. 固定研究协议

- 随机种子仍固定为 `2026`，快速迭代阶段不做多种子搜索。
- 候选开发只允许使用 Train、Train 内部 LOTO 和 Val。
- 当前 Test/Blind 已用于 v1/v2最终报告；迭代2必须建立新的锁定评测版本。新候选冻结前，不得反复查看旧或新 Test/Blind 指标并据此调参。
- 目标 SRCC `0.70` 指新的严格跨图幅验证指标，不指 Train SRCC，也不允许通过泄漏、改变划分或删除困难样本实现。
- 所有候选同时报告 SRCC、PLCC、MAE/RMSE、最差图幅指标和分攻击指标。
- 推理只能输入一个 `.gltf + .bin + textures`，不得输入 Clean、攻击标签、强度标签或配准差值。
- 退化类型和强度可以作为模型输出及训练辅助目标，但不能作为质量预测输入。
- 大数据、攻击资产和全量缓存只写入 `/Volumes/SANDISK-ELE/UrbanPhotoMeshQA-Data`。任何相关操作前先检查 `/Volumes/SANDISK-ELE`；未挂载时立即停止并提醒用户插盘。
- Mac 只做代码、清单、小规模审计和文档；全量生成、特征提取与训练在 RTX 4090 执行。
- 每项先做小型 Pilot，通过明确门槛后才扩至全量；所有新结果使用独立的 `ablations/iteration2_*` 或 `final/iteration2_*` 目录。

## 3. P0：重构机器伪 MOS（最高优先级）

### 3.1 审计现有 OQI v2

**状态：[x] 已验收**

逐建筑、逐退化、逐强度检查当前 Geometry、Texture 和 Overall 真值分布，重点回答：

- 六类退化的 light/medium/heavy 是否具有合理且相近的动态范围；
- 是否存在大量分数饱和在 `0` 或 `1`；
- 纹理错位、重影、细节损失是否被现有像素指标低估；
- 孔洞、QEM 和区域缺失是否被单个极端指标支配；
- 模型尺寸、面数、纹理分辨率是否系统性改变同等攻击的真值；
- `Overall=min(Geometry, Texture)` 是否造成不必要的梯度截断和分数塌缩。

验收输出：真值分布表、异常样本清单、每个指标对最终 OQI 的敏感度和至少 18 组可视化案例，不改变任何正式真值。

验收结果（2026-09-03）：

- 3493/3493条正式真值读取成功，Train/Val/Test/Blind计数保持`1518/760/607/608`，未修改任何v2分数；
- 孔洞攻击有`21.9%`有效样本OQI不高于`0.01`，确认存在低端饱和；
- 六类攻击heavy平均OQI从孔洞`0.086`到纹理错位`0.730`，跨攻击感知标尺需要重新校准；
- 几何分数由geometry fidelity主导`51.7%`、topology health主导`45.9%`，completeness仅主导`2.5%`；
- 已生成6类×3级共18个代表攻击及对应Clean的固定视角复核图。孔洞和区域缺失肉眼等级清楚；QEM、噪声、细节损失与错位需要多视图局部感知证据；
- 审计代码和结果位于`scripts/audit_objective_quality_targets_v2.py`与`artifacts/quality/ablations/iteration2_oqi_v2_audit/`。

### 3.2 建立 Machine Pseudo-MOS v3

**状态：[x] 已验收**

候选教师只用于离线真值生成：

- 几何教师：归一化表面距离、法线、完整性、拓扑健康和多尺度局部结构；
- 纹理教师：UV 表面采样、多视图结构/梯度指标，以及感知特征距离；
- 排序先验：同一建筑同一攻击应满足 `clean ≥ light ≥ medium ≥ heavy`；
- 跨攻击标尺：避免不同攻击因量纲不同而出现不合理的极端差异；
- 不把攻击类别直接写进分数公式，只允许用类别做分层校准审计，防止模型学习人为类别常数。

只比较三个透明且可解释的 Overall 方案：

1. 当前最小值 `min(G,T)`；
2. soft-min，保留最差分量主导但避免硬截断；
3. 有界的几何—纹理交互融合。

先在固定Pilot上由本轮执行者完成成对视觉合理性检查并保存可复核报告，用户当前无需参与逐例确认；这一步只做机器伪MOS质检，不冒充正式人工MOS。Pilot通过后生成全量v3，并冻结公式、常数、代码哈希和有序样本指纹。

验收门槛：

- 六类攻击的有效三级序列单调率为 `100%`；
- 非 no-op 样本不得出现异常集中于 `0/1`；
- 固定案例中明显更严重的退化不得获得更高分；
- 真值不由单一指标长期支配，所有权重和尺度可解释；
- Val/Test/Blind 不参与公式或参数选择。

验收结果（2026-09-04）：已在Iteration 2的`6289`条有效记录上生成正式Pseudo-MOS v3。原始三级单调率为`98.993%`，通过透明的同组累计退化负担包络处理后达到`100%`；公式常数只来自旧Train审计和固定18例Pilot，未使用新Val/Test/Blind模型结果。正式输出位于`artifacts/quality/final/iteration2_pseudo_mos_v3_seed2026/`。

### 3.3 与后续人工 MOS 兼容的标注体系

**状态：[x] 协议已冻结，人工标注后续执行**

**固定方案：人工界面使用5档评分；模型预测使用`[0,1]`连续质量分数；训练时同时增加5档序数分类辅助任务。**

主流主观质量实验通常让单个受试者选择5档或11档，但多人评分求平均后，MOS自然成为带小数的连续分数。因此本项目采用“双层标注”，而不是把训练真值强制离散化：

- 人工界面层：5档质量等级，便于用户稳定判断；
- 数据层：保存每位标注者的原始等级、置信度和备注；
- 真值层：多人等级均值形成连续MOS，并归一化为`[0,1]`；
- 模型层：连续质量回归为主，同时增加5档序数分类辅助头；
- 展示层：同时给出连续分数和质量档次。

建议5档定义为：`5 无明显缺陷`、`4 轻微但不影响使用`、`3 可见且有一定影响`、`2 明显影响使用`、`1 严重不可接受`。归一化公式为`(MOS-1)/4`。机器伪MOS v3沿用`[0,1]`，保证将来替换或混合人工MOS时不改模型输出接口。

## 4. P1：数据规模与划分升级

### 4.1 先扩大独立内容，不先堆攻击数量

**状态：[x] 已验收**

当前 Train 虽有1518条记录，但只有80栋独立建筑。优先增加：

- 不同图幅和区域的官方独立建筑；
- 不同体量、面数、连通分量、纹理分辨率和建筑形态；
- 原始摄影测量缺陷明显但仍可解析的真实模型。

阶段目标已确定为主数据集最终共`300`栋独立建筑，即在现有184栋基础上筛选并新增约116栋；Blind使用额外独立资源，不占这300栋额度。新增建筑必须按建筑分组划分，禁止同一建筑及其攻击跨Split。

已确定的扩容数据来源与存放规则：

- 源数据目录：`/Volumes/SANDISK-ELE/HK3D-Individualised`；
- 目标Clean数据必须存入`/Volumes/SANDISK-ELE/UrbanPhotoMeshQA-Data`下独立的Iteration 2目录；
- 建议目标目录：`/Volumes/SANDISK-ELE/UrbanPhotoMeshQA-Data/HK3D-Individualised-Iteration2-300`；
- 不覆盖或原地修改现有`HK3D-Individualised`、`HK3D-Individualised-Attack`和`HK3D-Individualised-Attack-v2`；
- 先按资产ID、glTF/BIN/贴图依赖和内容哈希与现有184栋去重，再从合格资产中选择约300栋；
- 选择时覆盖多个图幅、建筑形态、模型尺寸、面数、连通分量和纹理分辨率，不能只按文件遍历顺序取前300栋；
- 每个Clean资产保持可独立打开的`.gltf + .bin + textures`结构，并保存源路径、图幅、资产哈希与复制审计清单；
- 允许淘汰质量异常的原始Clean，但必须先分为`合格Clean`、`可用但含天然缺陷`和`不可用`三类；前两类保留类别标记，不可用资产仅在依赖缺失、无法解析、空Mesh或纹理/UV彻底不可用时排除，并逐项记录理由；
- 所有复制和全量解析在用户确认本方案后进行，且任何操作前必须确认`/Volumes/SANDISK-ELE`已挂载。

验收结果（2026-09-03）：从`2485`个候选资产中冻结主数据集`300`栋与额外Blind `40`栋；主集为Train/Val/Test=`210/45/45`，Blind来自完整隔离图幅`11-SW-3B`与`11-SW-4D`。本地和GPU均完成逐资产整包哈希、依赖、解析、UV、空Mesh及重复内容审计，最终`340/340`通过。另修复1处仅在Linux暴露的`.jpg/.JPG`引用大小写问题，未修改原始母库。

### 4.2 增加更接近实际的退化

**状态：[x] 第一阶段已验收**

在新增建筑到位后，再考虑：

- 几何＋纹理组合退化；
- 连续随机强度，而不只固定三级；
- 未参与训练的未知退化类型；
- 从真实异常模型整理的自然缺陷集。

单纯对现有80栋复制更多同类攻击不视为有效扩容。攻击包必须保持文件级真实性、可独立打开、位置固定而强度变化，并通过依赖和非 no-op 审计。

Iteration 2的Attack生成规则：

- 以选定的约300栋Clean资产为唯一输入，复用已验收的文件级攻击实现和大坐标安全导出逻辑；
- Attack输出使用全新目录，建议为`/Volumes/SANDISK-ELE/UrbanPhotoMeshQA-Data/HK3D-Individualised-Attack-Iteration2-300`；
- 第一阶段仍生成6类攻击×light/medium/heavy，便于与现有基线严格对照；
- 第二阶段才增加组合退化、连续强度和未知退化，不与第一阶段基线混在同一Manifest；
- 同一建筑、同一攻击的三级固定缺陷位置，只改变严重程度；纹理区域必须落在真实UV使用区；
- 每个攻击样本必须是可独立打开的资产包，并记录Clean来源哈希、攻击参数、随机种子和输出文件哈希；
- 生成后依次执行依赖完整性、可解析性、非空、非 no-op、三级唯一性和强度单调性审计；
- 攻击生成、全量审计和缓存提取只在RTX 4090服务器执行，本地移动硬盘接收最终数据与审计结果；
- 用户确认方案前，不复制Clean资产、不生成任何Attack、不创建上述目标数据目录。

第一阶段Pilot结果（2026-09-03）：从Train选择6栋、覆盖`56–4572`面及不同贴图规模，生成6类×3级共`108`个独立攻击包；`108/108`哈希、依赖和重载通过，36组建筑—攻击序列全部严格单调，无no-op。联系表已由执行者完成视觉复核。Pilot远端占用`860 MiB`，据此全量预计约`49 GiB`，GPU剩余空间满足生成要求。

第一阶段正式结果（2026-09-04）：生成`6120`个攻击包并完成一次全量文件审计；排除`76`条像素级纹理错位no-op和`5`条QEM重复/非单调记录后得到`6039`条正式攻击。随后根据UV表面和客观目标缓存再排除`90`条“文件变化但表面不可见”的no-op。最终有效数据为`6289`条（含`340`条Clean），Train/Val/Test/Blind=`3903/843/825/718`。后续不重复运行耗时全量审计，只复用哈希报告并做抽样一致性检查。

### 4.3 新的锁定划分

**状态：[x] 已冻结**

建立 Iteration 2 专用分组划分，并明确两种不同泛化目标：

- Train、Val、Test允许混合各个已有图幅，不要求相互跨图幅；划分单位是独立建筑，同一建筑及其Clean/全部攻击只能属于一个Split；
- 按建筑形态、面数、纹理分辨率、图幅和退化类型进行分层，使Train/Val/Test分布尽量一致；
- Blind使用Train/Val/Test从未出现过的新图幅，专门评价跨图幅泛化；
- Train内部仍可做按图幅LOTO诊断，但它是稳健性工具，不等同于正式Train/Val/Test划分规则。

正式划分协议确定为：

```text
主数据集300栋：Train 210、Val 45、Test 45
额外Blind：30–50栋，来自至少2个Train/Val/Test从未出现的完整图幅
```

现有184栋不继承旧Split标签，与新增资产合并后重新组织。Train/Val/Test可混合图幅，但建筑ID严格互斥，并按图幅、建筑形态、尺寸、面数、纹理分辨率和天然缺陷状态分层。Blind按图幅隔离且不占300栋额度。所有划分在查看新真值和模型结果前冻结；候选开发只加载Train/Val，唯一候选冻结后才一次性评测Test和Blind。

## 5. P2：质量评估与退化诊断联合模型

### 5.1 明确输出任务

**状态：[x] 第一阶段已实现**

单模型输出：

```text
Overall Pseudo-MOS
Geometry Quality
Texture Quality
Degradation Type（clean + 6类 + unknown）
Degradation Severity（连续值，同时保留light/medium/heavy统计）
16个Patch的Geometry / Texture / Overall
```

类型识别采用多标签或层级形式，以兼容组合退化；不得假定所有输入只含一种已知攻击。

### 5.2 多任务学习最小消融

**状态:[-] 已完成Train/Val最小消融，组合/unknown留待第二阶段**

只比较以下递进方案：

1. 质量回归基线；
2. `+` 同建筑排序损失；
3. `+` 退化类型辅助头；
4. `+` 连续强度辅助头；
5. `+` unknown/组合退化训练。

质量分数始终是主任务。辅助任务只有在提升LOTO与Val质量指标、且没有依赖攻击生成器伪影时才保留。

诊断指标：macro-F1、balanced accuracy、每类召回率和混淆矩阵。质量指标：OQI/Geometry/Texture的SRCC、PLCC和MAE。

### 5.3 有标签训练、锁定评测与无标签部署的边界

**状态：[x] 协议已落实**

当前总体设计合理，但必须区分三个阶段：

- 训练：输入单个Attacked或Clean glTF，监督信号是离线生成的Pseudo-MOS、等级、类型和强度；Clean—Attacked配对只发生在真值生成端，不作为部署模型的双输入；
- Val/Test/Blind评测：模型推理时仍看不到真值，但评测程序必须在预测完成后读取隐藏真值，才能计算SRCC、PLCC和MAE；
- 真实部署：没有真值，模型只输出预测分数、等级、退化诊断和局部热力图，此时不能声称计算了SRCC或准确率。

因此“测试阶段模型没有真值输入”是正确的；但“整个测试集不存在真值”则无法评价模型。Blind真值应对训练与选模流程隐藏，由最终评测脚本在冻结后使用。

可以加入无标签辅助任务帮助泛化，但不能替代有真值的最终评测。按优先级只验证：

1. 同一Mesh不同合法视角/轻微增强的质量预测一致性；
2. Face/Patch遮挡恢复或掩码特征重建；
3. 几何Patch与对应UV/多视图Patch的跨模态对比学习；
4. 无标签Clean/真实模型的质量表征预训练。

无标签辅助任务只能使用Train资产或另行声明的无标签预训练池，不得接触Test/Blind资产，防止测试域适配造成协议泄漏。是否保留由LOTO和Val OQI SRCC决定。

## 6. P3：优先解决跨图幅过拟合

### 6.1 建筑级与图幅级训练控制

**状态：[-] 正在执行**

- batch按建筑分组并平衡图幅、攻击类型和强度；
- 每栋建筑不能因攻击记录多而获得过高权重；
- 特征归一化只在Train拟合，并检查尺寸、面数、纹理分辨率泄漏；
- 继续使用Train图幅LOTO，主看平均图幅和最差图幅，而不是Train总体分数。

### 6.2 模型路线按成本递进

**状态：[-] 三层模型比较已完成，端到端微调尚未执行**

现有模型参数量审计：全局推理链约`2.63M`参数，其中Point Encoder约`0.44M`、Mesh Encoder约`0.82M`、MobileNetV3纹理特征约`0.93M`、Quality Head约`0.44M`；局部v2约`1.22M`。当前属于轻量模型，Mesh分支仅进行较浅的共享边消息传递，容量和跨模态交互均可能不足。

项目已结束最小可行性验证，本轮允许进入更完整的质量专用深层网络，但不把参数量本身当作贡献。正式模型比较已确定为三个主要层级，并在每层内部做最少必要消融：

1. 当前轻量模型＋Pseudo-MOS v3，作为同数据同协议基线；
2. GATv2 Mesh＋较强纹理编码器：以三角面为节点，共享边、法线夹角、边长和UV seam作为边特征，学习邻居权重；
3. Face→Patch→Building层次Transformer＋几何纹理Cross-Attention：建立局部缺陷到全局质量的可解释聚合，并让Face/Patch几何Token与对齐UV、多视图纹理Token双向交互。

三个层级均使用质量回归、5档序数分类、退化类型和强度的共享主干多任务训练。随后只对胜出层级验证Train图幅间质量感知style mixup、无标签辅助任务、部分解冻和端到端质量微调，避免组合数量失控。

建议建立Small/Medium两个容量档：Small用于快速消融，Medium作为正式候选。优先把参数规模提高到约`10M–30M`，是否继续扩大由Val和最差图幅表现决定，而不是预设越大越好。每个增强模块必须与相同真值、相同划分和相同训练预算的基线比较，同时报告参数量、FLOPs、显存、推理时间及SRCC增益。

禁止把Locked Blind作为无标签目标域做域适配。部分解冻只有在冻结Base已证明受限时才进入，并必须监控Train—Val间隙。

### 6.3 晋级门槛

**状态：[x] Iteration 2局部真值已生成并验收**

阶段门槛建议：

- Gate A：Val OQI SRCC达到 `0.60`，且最差Train-LOTO图幅达到 `0.55`；
- Gate B：Val达到 `0.65`，最差图幅达到 `0.60`；
- 最终目标：严格跨图幅Val OQI SRCC至少 `0.70`，Geometry和Texture均不得低于 `0.60`；
- 同时要求Val MAE改善，退化类型macro-F1不低于 `0.80`；
- 若SRCC提升但机器伪MOS案例合理性下降，不得晋级。

`0.70`是研究目标而非通过修改划分必须实现的结果。若连续两阶段无法达到，应报告瓶颈并优先扩充独立建筑，不进行大规模超参数搜索。

## 7. P4：局部质量继续优化

### 7.1 局部真值与全局真值统一

**状态：[-] 第一轮模型已完成，局部几何仍需提升**

局部Patch真值同步升级到Pseudo-MOS v3，检查16个Patch聚合后与全局Geometry、Texture和Overall的一致性。比较面积加权、最差Patch主导和可学习可靠性聚合，但聚合器必须只用Train/Val选择。

### 7.2 提升局部几何与可解释性

**状态：[ ] 待确认**

- 增加多尺度测地邻域、边界/非流形和曲率变化证据；
- 保持Patch—Face—UV对应，不回退到仅欧氏点Patch；
- 输出Patch置信度和有效纹理覆盖率；
- 用已知攻击区域验证定位命中率、IoU或Top-k Recall，而不只看Patch分数SRCC。

目标：Val局部Geometry SRCC从`0.381`提升至至少`0.55`，Texture保持不低于`0.60`，Overall达到至少`0.55`。

## 8. P5：防止攻击生成器捷径

**状态：[ ] 待确认**

- 同类攻击使用多种实现或参数分布，避免识别固定代码指纹；
- 训练中保留一部分攻击参数区间和至少一种退化实现作为unseen degradation；
- 比较“攻击类型识别正确但质量排序错误”的样本；
- 检查文件大小、纹理编码格式、目录名和metadata是否意外泄漏标签；
- 正式推理不得读取`metadata.json`中的攻击字段。

只有未知退化质量排序和已知退化同时改善，才能声称模型学到了质量，而不只是攻击分类。

## 9. P6：最终冻结与发布

**状态：[ ] 待确认**

唯一候选需在解锁前保存：

- 模型与配置SHA256；
- 数据Manifest、伪MOS、特征缓存的有序样本指纹；
- Train/LOTO/Val完整结果及晋级依据；
- 推理代码版本和缓存—在线等价审计；
- 明确写入“锁定后禁止继续调参”。

随后只执行一次Locked Test/Blind评测。若结果未达`0.70`，如实报告，不允许回看锁定集继续修改同一发布版本；后续改进必须建立新的数据版本和预注册式实验方案。

## 10. 建议执行顺序

```text
P0.1 现有真值审计
→ P0.2 小规模Pseudo-MOS v3 Pilot
→ 自动视觉合理性复核并保存待查报告
→ P1 独立建筑与新锁定划分
→ 全量Pseudo-MOS v3
→ P2 多任务最小消融
→ P3 跨图幅泛化递进实验
→ P4 局部质量升级
→ P5 未知退化与捷径审计
→ P6 唯一候选冻结及一次性锁定评测
```

## 11. 执行边界

- 不修改现有v1/v2正式真值、模型或发布目录；
- 新攻击、缓存与GPU训练只在对应Pilot通过后执行；
- 不读取或汇总Test/Blind新结果；
- 不下载外部大数据集；
- 不根据旧Blind逐样本误差反向调参。

## 12. 2026-09-04阶段结果（仅Train/Val）

本阶段全程固定随机种子`2026`，只用Train训练、Val选模，未加载Test/Blind特征进行候选比较。

| 候选 | Val OQI MAE↓ | PLCC↑ | SRCC↑ | Geometry SRCC↑ | Texture SRCC↑ |
|---|---:|---:|---:|---:|---:|
| 轻量四分支＋Pseudo-MOS v3 | 0.190 | 0.615 | 0.616 | 0.654 | 0.636 |
| GATv2 Mesh＋EfficientNet-B0纹理 | 0.174 | 0.654 | 0.645 | 0.700 | 0.670 |
| Face→Patch→Building Transformer＋Cross-Attention | 0.172 | 0.651 | 0.656 | 0.694 | 0.656 |
| GATv2＋层次模型固定等权集成 | 0.166 | 0.672 | 0.667 | 0.704 | 0.684 |
| 三模型固定等权集成（含Clean/light加权模型） | 0.169 | 0.681 | 0.678 | 0.714 | 0.693 |
| 面向Patch的空间纹理层次模型 | 0.175 | 0.638 | 0.631 | 0.686 | 0.696 |
| 四模型固定等权集成（加入空间纹理模型） | 0.165 | 0.696 | 0.688 | 0.729 | 0.705 |
| 特征增强层次模型（单模型） | 0.167 | 0.684 | 0.678 | **0.765** | **0.741** |
| 五模型固定等权集成（最终候选） | **0.163** | **0.703** | **0.696** | 0.747 | 0.724 |

已验证但淘汰的方向：仅同建筑同攻击排序（SRCC`0.651`、MAE`0.201`），以及Clean/light加权单模型（SRCC`0.656`、MAE`0.190`）。后者只因预测互补性进入固定三模型集成，不作为独立候选。

Val诊断显示：`geometry_hole`最好（OQI SRCC`0.806`），`texture_misalignment`最弱（`0.446`）；light/medium/heavy分别为`0.525/0.664/0.774`。Clean真值为常数，分组内相关系数无定义（程序记为0），但其MAE`0.299`揭示模型对高质量端仍有系统性低估。下一轮优先提升轻度纹理错位和高质量端校准，而不是扩大无依据的参数搜索。

进一步审计得到三模型集成Train OQI SRCC=`0.949`、Val=`0.678`，泛化差距约`0.271`，因此不再通过扩大模型容量解决。新增的空间Patch纹理表征单模型总体排序较弱，但Texture SRCC达到`0.696`，具有互补性；训练期特征噪声与纹理Token丢弃使单模型Geometry/Texture SRCC提升到`0.765/0.741`。二者依次纳入预先固定的等权平均后，五模型候选达到Val OQI MAE/PLCC/SRCC=`0.163/0.703/0.696`，接近`0.70`目标，同时未搜索集成权重。

当前主要剩余瓶颈仍是轻度样本（五模型OQI SRCC=`0.555`）和纹理错位（`0.462`）；重度样本已达到`0.800`。五模型是精度上限候选，但部署成本较高；进入锁定集前应同时保留特征增强单模型作为可蒸馏教师/轻量候选，并完成最终在线推理等价审计。Test/Blind仍未加载或评测。

注意：当前批量冻结特征与旧逐样本提取的32例最大绝对差为`0.003426`，可用于快速消融，但尚不满足正式发布的缓存—在线完全等价要求。唯一候选冻结前必须用最终单模型推理链重新提取或完成严格等价验证。

局部Patch阶段结果（2026-09-04）：已为340栋生成固定16槽位的Face→Patch→UV映射，平均UV有效面率`99.86%`、面覆盖率`100%`。局部几何真值`3395`条、局部纹理真值`3234`条；纹理有效Patch覆盖率`99.96%`，`10491/10491`个可观察三级Patch序列单调。最终`6289`条局部目标与正式Manifest严格对齐。

仅用Train/Val训练的固定分工组合达到Val Patch Geometry/Texture/Overall SRCC=`0.496/0.623/0.545`，相对旧v2的`0.381/0.602/0.422`分别提升`+0.115/+0.021/+0.123`。Overall已接近`0.55`门槛，Texture达标，Geometry仍低于`0.55`，下一轮应加强面级几何消息传递与攻击区域监督，不再扩展纹理编码器。
